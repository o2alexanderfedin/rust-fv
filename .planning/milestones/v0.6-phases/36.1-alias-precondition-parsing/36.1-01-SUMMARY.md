---
phase: 36.1-alias-precondition-parsing
plan: "01"
subsystem: formal-verification
tags: [rust, alias-precondition, hir-scanner, vcgen, z3, unsafe-verification]

# Dependency graph
requires:
  - phase: 36-summary-contract-inference
    provides: FunctionSummary.alias_preconditions field always-empty gap (identified in audit)
  - phase: 34-alias-vc-injection
    provides: vcgen alias VC emission (VcKind::PointerAliasing) and AliasPrecondition struct
provides:
  - HIR scanner in callbacks.rs parses rust_fv::unsafe_requires:: prefix into HirContracts.unsafe_requires
  - parse_alias_preconditions() converts unsafe_requires strings to AliasPrecondition entries via source_to_idx map
  - FunctionSummary.alias_preconditions now populated from HIR (not always vec![])
  - 4 unit tests for parse_alias_preconditions in callbacks.rs #[cfg(test)] block
  - 4 driver-level integration tests in alias_precondition_e2e.rs confirming full pipeline
affects:
  - phase-37-cross-crate-recursion (alias preconditions now flow through full driver pipeline)
  - any cross-crate callers of functions annotated with #[unsafe_requires(!alias(p, q))]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "HirContracts returns raw data; hir_contracts_to_ir() converts to IR Contracts separately — preserves unsafe_requires across the boundary"
    - "source_to_idx map built by inverting build_source_names() output against mir.args_iter() — maps source param name to zero-based index"
    - "Alias VC is a SAT-check (assert eq = violation). UNSAT requires caller precondition _1 != _2 in scope"

key-files:
  created:
    - crates/driver/tests/alias_precondition_e2e.rs
  modified:
    - crates/driver/src/callbacks.rs

key-decisions:
  - "Changed extract_contracts() return type from HashMap<LocalDefId, Contracts> to HashMap<LocalDefId, HirContracts> so unsafe_requires is preserved across the HIR->IR boundary — minimal invasiveness, only 2 call-sites updated"
  - "Added hir_contracts_to_ir() helper for converting HirContracts to IR Contracts — avoids code duplication at 2 call sites"
  - "UNSAT alias VC test requires caller precondition (_1 != _2) — alias VC asserts eq(a,b) which is SAT by default without constraints; the plan's 'distinct locals = UNSAT' assumption only holds when caller has an anti-alias precondition"
  - "parse_alias_preconditions() accepts both !alias(p, q) and alias(p, q) — matches existing extract_alias_preconditions() semantics in unsafe_analysis.rs"

patterns-established:
  - "HirContracts-to-IR conversion split: extract raw data in HirContracts, convert with hir_contracts_to_ir() at use site"
  - "source_to_idx inversion pattern: build_source_names() -> invert against args_iter() -> zero-based param index map"

requirements-completed: [ALIAS-01, ALIAS-02]

# Metrics
duration: 11min
completed: 2026-03-01
---

# Phase 36.1 Plan 01: Alias Precondition Parsing Summary

**HIR scanner gap closed: callbacks.rs now parses !alias(p, q) from unsafe_requires attributes and populates FunctionSummary.alias_preconditions via source_to_idx map, completing the Phase 34 vcgen pipeline**

## Performance

- **Duration:** 11 min
- **Started:** 2026-03-01T09:26:02Z
- **Completed:** 2026-03-01T09:37:22Z
- **Tasks:** 2
- **Files modified:** 2 (1 created, 1 modified)

## Accomplishments
- Extended HirContracts struct with `unsafe_requires: Vec<String>` field and added `rust_fv::unsafe_requires::` parse arm in extract_contracts()
- Changed extract_contracts() to return `HashMap<LocalDefId, HirContracts>` and added `hir_contracts_to_ir()` to preserve unsafe_requires across the HIR->IR boundary
- Added `parse_alias_preconditions()` private function in callbacks.rs that converts unsafe_requires strings to AliasPrecondition entries using source_to_idx map
- Replaced hardcoded `alias_preconditions: vec![]` with parsed values in FunctionSummary construction
- Created alias_precondition_e2e.rs with 4 driver-level integration tests confirming full pipeline: VC generated, SAT for aliased args, UNSAT with distinct args + caller precondition, zero VCs for empty preconditions

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend HIR scanner to capture unsafe_requires and parse alias preconditions** - `f439541` (feat)
2. **Task 2: Integration test — full pipeline alias VC via driver path** - `8232f9c` (test)

**Plan metadata:** (pending docs commit)

## Files Created/Modified
- `crates/driver/src/callbacks.rs` - Added HirContracts.unsafe_requires, extract_contracts returns HirContracts, hir_contracts_to_ir() helper, parse_alias_preconditions() function, 4 unit tests in #[cfg(test)] block
- `crates/driver/tests/alias_precondition_e2e.rs` - 4 integration tests exercising full driver -> vcgen -> alias VC pipeline

## Decisions Made
- extract_contracts() return type changed from IR Contracts to HirContracts to preserve unsafe_requires across HIR->IR boundary (2 call-site updates in after_analysis)
- hir_contracts_to_ir() added as separate converter to avoid code duplication
- UNSAT alias VC test uses caller precondition `_1 != _2` — alias VC semantics are SAT-based (assert equality = violation); without an anti-alias constraint, Z3 freely satisfies the equality

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected UNSAT integration test to add caller anti-alias precondition**
- **Found during:** Task 2 (alias_precondition_e2e.rs)
- **Issue:** Plan implied UNSAT test passes distinct args only, but alias VC is SAT-based (asserts _1 == _2); with no constraint, Z3 satisfies it regardless of distinct locals
- **Fix:** Added `caller.contracts.requires.push(SpecExpr { raw: "_1 != _2" })` to Test 3 so the VC assumption blocks the alias
- **Files modified:** crates/driver/tests/alias_precondition_e2e.rs
- **Verification:** Test passes (UNSAT confirmed with caller precondition)
- **Committed in:** 8232f9c (Task 2 commit)

**2. [Rule 1 - Bug] Changed extract_contracts return type to preserve unsafe_requires**
- **Found during:** Task 1 (callbacks.rs implementation)
- **Issue:** Plan said "return type unchanged" but IR Contracts has no unsafe_requires field — the data would be lost before contract_db loop
- **Fix:** Changed return type to HirContracts, added hir_contracts_to_ir() converter
- **Files modified:** crates/driver/src/callbacks.rs
- **Verification:** All 441 driver tests pass
- **Committed in:** f439541 (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (both Rule 1 - Bug)
**Impact on plan:** Both auto-fixes essential for correctness. No scope creep.

## Issues Encountered
- Plan's interface note said extract_contracts return type unchanged, but IR Contracts lacks unsafe_requires — resolved by returning HirContracts and adding hir_contracts_to_ir() converter

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- ALIAS-01 and ALIAS-02 gap closure complete: HIR scanner populates alias_preconditions in FunctionSummary
- Phase 37 (cross-crate recursion / XCREC) can proceed as planned
- Full regression suite clean: 441 driver tests + 1236 analysis tests pass; clippy -D warnings clean

## Self-Check: PASSED

- FOUND: crates/driver/src/callbacks.rs
- FOUND: crates/driver/tests/alias_precondition_e2e.rs
- FOUND: commit f439541 (Task 1)
- FOUND: commit 8232f9c (Task 2)

---
*Phase: 36.1-alias-precondition-parsing*
*Completed: 2026-03-01*

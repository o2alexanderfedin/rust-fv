---
phase: 37-cross-crate-scc-detection
plan: 03
type: execute
wave: 3
depends_on:
  - "37-02"
files_modified:
  - crates/analysis/tests/recursion_verification.rs
autonomous: true
requirements:
  - XCREC-01
  - XCREC-02

must_haves:
  truths:
    - "cargo verify detects a mutually recursive cycle between functions in two different crates and reports the SCC members by name"
    - "User can attach #[decreases(expr)] to functions participating in a cross-crate recursive cycle and cargo verify checks that the termination measure decreases across crate boundaries"
    - "Cross-crate SCC detection uses exported symbol contracts so crate boundaries are transparent to Tarjan's algorithm"
    - "Single-crate recursive function verification remains GREEN with no regressions"
  artifacts:
    - path: "crates/analysis/tests/recursion_verification.rs"
      provides: "Integration tests for cross-crate SCC detection + termination verification"
      contains: "cross_crate_scc"
  key_links:
    - from: "crates/analysis/tests/recursion_verification.rs"
      to: "vcgen::generate_vcs"
      via: "build ContractDatabase with cross-crate callee, build IR Function calling it, call generate_vcs, verify Termination VCs in results"
      pattern: "generate_vcs.*contract_db"
---

<objective>
Add end-to-end integration tests in recursion_verification.rs that exercise the full cross-crate SCC detection pipeline: ContractDatabase → from_functions_with_cross_crate_db → detect_recursion → generate_termination_vcs → SMT-LIB → Z3.

Purpose: XCREC-01 and XCREC-02 success criteria require a demonstration that `cargo verify` detects cross-crate mutual recursion and checks termination measures. The integration tests simulate a two-crate scenario: one "local" function and one "remote" function registered in a ContractDatabase. The tests verify that Z3 reports UNSAT for well-decreasing cross-crate measures and SAT (counterexample) for non-decreasing measures.

Output: 4 integration tests appended to recursion_verification.rs. All pass with Z3.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-cross-crate-scc-detection/37-01-SUMMARY.md
@.planning/phases/37-cross-crate-scc-detection/37-02-SUMMARY.md
@crates/analysis/tests/recursion_verification.rs
@crates/analysis/src/recursion.rs
@crates/analysis/src/vcgen.rs
@crates/analysis/src/contract_db.rs
@crates/analysis/src/call_graph.rs
</context>

<interfaces>
<!-- Key types from the integration test file. -->

From crates/analysis/tests/recursion_verification.rs (top):
```rust
use rust_fv_analysis::ir::*;
use rust_fv_analysis::vcgen::{self, VcKind};
use rust_fv_solver::Z3Solver;

fn solver_or_skip() -> Z3Solver { ... }
fn script_to_smtlib(script: &rust_fv_smtlib::script::Script) -> String { ... }
```

The test file already has helpers. New tests will use:
```rust
use rust_fv_analysis::contract_db::{ContractDatabase, FunctionSummary};
```

Key vcgen entry point:
```rust
pub fn generate_vcs(
    func: &Function,
    contract_db: Option<&ContractDatabase>,
    ghost_pred_db: Option<&GhostPredicateDatabase>,
    source_locations: Option<&HashMap<(usize, usize), (String, usize, usize)>>,
) -> Vec<VerificationCondition>
```

The tests will call `vcgen::generate_vcs(&local_fn, Some(&db), None, None)` and check the returned VCs.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add cross-crate SCC detection integration tests</name>
  <files>crates/analysis/tests/recursion_verification.rs</files>
  <action>
    Append 4 new test functions to the end of recursion_verification.rs. Add `use rust_fv_analysis::contract_db::{ContractDatabase, FunctionSummary};` at the top of the file if not already present.

    **Test 1: test_cross_crate_scc_detection_reports_members**
    - Purpose: XCREC-01 — SCC detected and group members reported by name
    - Setup:
      * Local function "local_crate::foo" with params=[_1:I32], contracts.decreases=Some("_1"), basic block Terminator::Call{func:"dep_crate::bar", args:[Copy(_1)], dest:_0, target:1}
      * DB entry "dep_crate::bar": param_names=["_1"], param_types=[I32], return_ty=I32, contracts.decreases=Some(SpecExpr{raw:"foo"}), is_inferred=false, alias_preconditions=[]
        (The raw decreases text "foo" contains the in-crate function normalized name — triggers back-edge)
      * Call `vcgen::generate_vcs(&foo, Some(&db), None, None)`
      * Assert: result contains at least one VC with `vc_kind == VcKind::Termination`
      * Assert: vc description contains "foo" or "bar" (member names reported)

    **Test 2: test_cross_crate_termination_decreasing_verifies_unsat**
    - Purpose: XCREC-02 — well-decreasing measure verifies (Z3 returns UNSAT)
    - Setup: Same as Test 1 but foo's call passes `Operand::BinaryOp` is not directly available in IR; instead create a local `_2 = _1 - 1` via Statement::Assign in the call block, then Terminator::Call with args=[Copy(_2)]
      NOTE: Since BinaryOp is encoded at the statement level, setup the call block as:
      BasicBlock {
          statements: [Statement::Assign(Place::local("_2"), Rvalue::BinaryOp(BinOp::Sub, Copy(_1), Constant(Int(1, I32))))],
          terminator: Terminator::Call{func:"dep_crate::bar", args:[Copy(_2)], dest:_0, target:1}
      }
    - Preconditions: contracts.requires=[SpecExpr{raw:"_1 >= 0"}]
    - Run Z3 on the Termination VC(s) found
    - Expected: Z3 returns UNSAT (measure _2 = _1-1 < _1 with _1 >= 0 is provable)
    - Use `solver_or_skip()` + `script_to_smtlib` + `solver.check_sat(smtlib)` pattern from existing tests

    **Test 3: test_cross_crate_termination_non_decreasing_produces_sat**
    - Purpose: XCREC-02 — non-decreasing measure produces SAT counterexample
    - Setup: Same as Test 2 but call args=[Copy(_1)] (passes _1 unchanged, not _1-1)
    - No preconditions
    - Run Z3 on Termination VC
    - Expected: Z3 returns SAT (counterexample: _1 is not less than _1)

    **Test 4: test_single_crate_recursion_regression**
    - Purpose: Success criterion 4 — single-crate recursion remains GREEN
    - Setup: Standard factorial from existing test helpers (or duplicate the fixture inline)
    - Call `vcgen::generate_vcs(&factorial, None, None, None)`
    - Assert: result contains VC with `vc_kind == VcKind::Termination`
    - No regression from cross-crate changes

    Implementation notes:
    - Reuse existing `make_function` pattern for building IR functions (it's in recursion.rs mod tests as a private helper — redefine inline in the test or use pub helper)
    - The `Function` struct constructor is available via `rust_fv_analysis::ir::*`
    - Use `Contracts { decreases: Some(SpecExpr { raw: "...".to_string() }), ..Default::default() }` for contracts
    - For Test 2/3, check UNSAT/SAT using the same pattern as existing recursion_verification.rs tests (look at how `test_factorial_verifies_termination` works in that file)
  </action>
  <verify>
    <automated>cargo test -p rust_fv_analysis --test recursion_verification 2>&1 | tail -20</automated>
  </verify>
  <done>
    4 new integration tests added to recursion_verification.rs. All pass (or Z3-skip gracefully if Z3 not available). Tests 1 and 4 pass without Z3. Tests 2 and 3 pass with Z3 (UNSAT and SAT respectively). All previously passing integration tests in recursion_verification.rs remain GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final validation — all tests green, lints clean, requirements traced</name>
  <files>crates/analysis/tests/recursion_verification.rs</files>
  <action>
    Run the full validation suite:

    1. `cargo test -p rust_fv_analysis 2>&1 | tail -20` — all unit tests GREEN
    2. `cargo test -p rust_fv_analysis --test recursion_verification 2>&1 | tail -20` — all 4 new integration tests + existing tests GREEN
    3. `cargo clippy -p rust_fv_analysis -- -D warnings 2>&1` — zero warnings
    4. `cargo clippy -p rust_fv_driver -- -D warnings 2>&1` — zero warnings

    If any test fails:
    - Check DB entry field names against current FunctionSummary struct (fields may have changed in earlier phases)
    - Check vcgen::generate_vcs signature — confirm parameter order matches current code
    - Check that `Contracts::default()` includes `is_inferred: false` and `fn_specs: vec![]` (they do per existing test helpers)

    Update REQUIREMENTS.md to mark XCREC-01 and XCREC-02 as Complete.
    Update ROADMAP.md Phase 37 status to Complete.
    Update STATE.md current position.

    Commit all changes: `git add -A && git commit -m "feat(37): cross-crate SCC detection + termination verification (XCREC-01, XCREC-02)"`
  </action>
  <verify>
    <automated>cargo test -p rust_fv_analysis 2>&1 | grep -E "test result|FAILED|error"</automated>
  </verify>
  <done>
    All tests GREEN. clippy clean. REQUIREMENTS.md shows XCREC-01 and XCREC-02 as Complete. ROADMAP.md Phase 37 marked Complete. Changes committed to git.
  </done>
</task>

</tasks>

<verification>
cargo test -p rust_fv_analysis 2>&1 | tail -20
cargo test -p rust_fv_analysis --test recursion_verification 2>&1 | tail -10
cargo clippy -p rust_fv_analysis -- -D warnings 2>&1
</verification>

<success_criteria>
1. `cargo test -p rust_fv_analysis --test recursion_verification` passes with 4 new tests:
   - test_cross_crate_scc_detection_reports_members (XCREC-01)
   - test_cross_crate_termination_decreasing_verifies_unsat (XCREC-02, Z3-dependent)
   - test_cross_crate_termination_non_decreasing_produces_sat (XCREC-02, Z3-dependent)
   - test_single_crate_recursion_regression (success criterion 4)
2. All pre-existing recursion_verification.rs tests remain GREEN
3. `cargo test -p rust_fv_analysis` (all unit tests) passes with zero failures
4. `cargo clippy -p rust_fv_analysis -- -D warnings` zero warnings
5. REQUIREMENTS.md updated: XCREC-01 and XCREC-02 marked Complete
</success_criteria>

<output>
After completion, create `.planning/phases/37-cross-crate-scc-detection/37-03-SUMMARY.md`
</output>

---
phase: 51-core-language-features-i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/analysis/src/ir.rs
  - crates/analysis/src/vcgen.rs
  - crates/analysis/src/encode_sort.rs
  - crates/analysis/src/monomorphize.rs
  - crates/analysis/src/spec_parser.rs
  - crates/driver/src/mir_converter.rs
  - crates/analysis/tests/const_generic_tests.rs
autonomous: true
requirements: [LANG-01]

must_haves:
  truths:
    - "A function with const generic parameter <const N: usize> has N available as an SMT integer constant in verification"
    - "Spec expressions like #[requires(N > 0)] reference const generic parameters by name"
    - "Monomorphization substitutes concrete const generic values at call sites"
    - "Array type [T; N] with const generic N uses N as symbolic length bound"
  artifacts:
    - path: "crates/analysis/src/ir.rs"
      provides: "Ty::ConstGeneric(String, Ty) variant + ConstGenericParam in GenericParam + all new VcKind variants (UnionAccess, DropOrder, PinSafety) for phase 51"
      contains: "ConstGeneric"
    - path: "crates/analysis/src/encode_sort.rs"
      provides: "SMT sort encoding for Ty::ConstGeneric and Ty::Union"
      contains: "ConstGeneric"
    - path: "crates/driver/src/mir_converter.rs"
      provides: "MIR extraction of const generic parameters from GenericParamDefKind::Const"
      contains: "GenericParamDefKind::Const"
    - path: "crates/analysis/src/monomorphize.rs"
      provides: "Const generic value substitution during monomorphization"
      contains: "ConstGeneric"
    - path: "crates/analysis/src/spec_parser.rs"
      provides: "Const generic parameter names resolvable in spec expressions"
      contains: "const_generic"
    - path: "crates/analysis/tests/const_generic_tests.rs"
      provides: "Unit tests for const generic encoding and verification"
      min_lines: 50
  key_links:
    - from: "crates/driver/src/mir_converter.rs"
      to: "crates/analysis/src/ir.rs"
      via: "Ty::ConstGeneric construction from MIR GenericParamDefKind::Const"
      pattern: "ConstGeneric"
    - from: "crates/analysis/src/monomorphize.rs"
      to: "crates/analysis/src/ir.rs"
      via: "Ty::ConstGeneric substitution with concrete values"
      pattern: "ConstGeneric"
    - from: "crates/analysis/src/spec_parser.rs"
      to: "crates/analysis/src/ir.rs"
      via: "Const generic names resolved as SMT integer constants in specs"
      pattern: "const_generic"
---

<objective>
Add IR type foundations for all Phase 51 features and implement LANG-01 const generic parameter verification.

Purpose: Const generics are pervasive in Rust (array sizes, buffer lengths, generic parameters). Without SMT encoding, functions like `fn first<const N: usize>(arr: [T; N])` cannot be verified. This plan also establishes the IR variants needed by Plans 02 and 03.

Output: New Ty::ConstGeneric variant, VcKind variants for unions/Drop/Pin, const generic MIR extraction, SMT encoding, monomorphization, spec parser support, and tests.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-core-language-features-i/51-CONTEXT.md

@crates/analysis/src/ir.rs
@crates/analysis/src/encode_sort.rs
@crates/driver/src/mir_converter.rs
@crates/analysis/src/monomorphize.rs
@crates/analysis/src/spec_parser.rs
@crates/analysis/src/vcgen.rs

<interfaces>
<!-- Key types and contracts the executor needs. -->

From crates/analysis/src/ir.rs:
```rust
pub struct GenericParam {
    pub name: String,
    pub trait_bounds: Vec<String>,
}

pub enum Ty {
    // ... existing variants ...
    Generic(String),
    // Add: ConstGeneric(String, Ty) — name + underlying type (e.g., usize)
}

pub struct Function {
    pub generic_params: Vec<GenericParam>,
    // ... many fields ...
    pub refcell_ghost_states: Vec<RefCellGhostState>,
    pub maybeuninit_ghost_states: Vec<MaybeUninitGhostState>,
}
```

From crates/driver/src/mir_converter.rs (line 60-69):
```rust
use rustc_middle::ty::GenericParamDefKind;
// Currently filters: if !matches!(param.kind, GenericParamDefKind::Type { .. }) { continue; }
// Must extend to handle GenericParamDefKind::Const
```

From crates/analysis/src/monomorphize.rs:
```rust
// Handles Ty::Generic(name) -> concrete substitution
// Must extend for Ty::ConstGeneric(name, ty) -> concrete value
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IR type extensions + const generic MIR extraction</name>
  <files>crates/analysis/src/ir.rs, crates/analysis/src/vcgen.rs, crates/driver/src/mir_converter.rs, crates/analysis/src/encode_sort.rs, crates/analysis/tests/const_generic_tests.rs</files>
  <behavior>
    - Test: Ty::ConstGeneric("N", Ty::Uint(UintTy::Usize)) constructs and pattern-matches correctly
    - Test: Ty::Union("U", fields) constructs with overlapping field representation
    - Test: VcKind::UnionAccess, VcKind::DropOrder, VcKind::PinSafety exist and have correct severity/diagnostic codes
    - Test: encode_sort for Ty::ConstGeneric returns Int sort (for integer const generics) or Bool sort
    - Test: encode_sort for Ty::Union returns BitVec of max field size
    - Test: MIR converter extracts GenericParamDefKind::Const as ConstGenericParam in generic_params
    - Test: Function with const generic has it accessible in generic_params with is_const flag
  </behavior>
  <action>
    1. In ir.rs, add to Ty enum:
       - `ConstGeneric(String, Box<Ty>)` — name and underlying type (usize, u8, bool, etc.)
       - `Union(String, Vec<(String, Ty)>)` — name and fields with overlapping storage

    2. In ir.rs, extend GenericParam:
       - Add `is_const: bool` field (default false for existing type params)
       - Add `const_ty: Option<Ty>` field for the const parameter's type

    3. In ir.rs, add to Function struct:
       - `union_ghost_states: Vec<UnionGhostState>` — for LANG-03 (Plan 02 will populate)
       - `pin_ghost_states: Vec<PinGhostState>` — for LANG-05 (Plan 03 will populate)
       - `drop_locals: Vec<DropLocalInfo>` — for LANG-04 (Plan 03 will populate)

    4. In ir.rs, add new structs:
       - `UnionGhostState { local_name: String, union_name: String, fields: Vec<(String, Ty)> }`
       - `PinGhostState { local_name: String, inner_ty: Ty }`
       - `DropLocalInfo { local_name: String, ty: Ty, has_drop: bool, drop_order: usize }`

    5. In vcgen.rs VcKind enum, add:
       - `UnionAccess` — reinterpretation cast safety for union field reads (V130)
       - `DropOrder` — drop scope-exit ordering verification (V140)
       - `PinSafety` — Pin move-prevention invariant (V150)
       Update VcKind severity/diagnostic methods for new variants.

    6. In encode_sort.rs, add arms for:
       - `Ty::ConstGeneric(_, ty)` — map to SMT sort based on underlying type (Int for integer types, Bool for bool)
       - `Ty::Union(_, fields)` — encode as BitVec of max(field_sizes) * 8

    7. In mir_converter.rs, modify the generic param extraction loop (around line 60-69):
       - Currently skips non-Type params with `if !matches!(param.kind, GenericParamDefKind::Type { .. }) { continue; }`
       - Add handling for `GenericParamDefKind::Const { .. }` — extract name, map type to IR Ty, set `is_const: true`
       - Keep skipping Lifetime params

    8. Update all existing Function construction sites in vcgen.rs tests to include new empty vec fields:
       `union_ghost_states: vec![], pin_ghost_states: vec![], drop_locals: vec![]`

    9. Write tests in const_generic_tests.rs verifying IR construction, sort encoding, and MIR extraction.
  </action>
  <verify>
    <automated>cd /Users/alexanderfedin/Projects/hapyy/rust-fv && cargo test --lib -p rust-fv-analysis const_generic -- --nocapture 2>&1 | tail -20 && cargo test --test const_generic_tests -p rust-fv-analysis -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>Ty::ConstGeneric and Ty::Union variants exist in IR. VcKind has UnionAccess/DropOrder/PinSafety. MIR converter extracts const generic params. encode_sort handles new Ty variants. All tests pass. cargo clippy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Const generic monomorphization + spec parser + VCGen wiring</name>
  <files>crates/analysis/src/monomorphize.rs, crates/analysis/src/spec_parser.rs, crates/analysis/src/vcgen.rs, crates/analysis/src/encode_term.rs, crates/analysis/tests/const_generic_tests.rs</files>
  <behavior>
    - Test: Monomorphization substitutes Ty::ConstGeneric("N", usize) with concrete value 5 at call site
    - Test: Array type [T; N] with const generic N=10 has length bound asserted as N==10 in SMT
    - Test: Spec expression `N > 0` resolves const generic N as SMT integer constant
    - Test: Function fn first<const N: usize>(arr: [T; N]) with #[requires(N > 0)] generates precondition VC with N as SMT variable
    - Test: Call site with N=0 produces precondition violation (SAT counterexample)
    - Test: Call site with N=5 satisfies precondition (UNSAT)
  </behavior>
  <action>
    1. In monomorphize.rs, extend substitution logic:
       - Handle `Ty::ConstGeneric(name, ty)` — when monomorphizing a call site with concrete const args, replace with the concrete value
       - Add `const_generic_values: HashMap<String, u128>` to MonomorphizationContext (or equivalent)
       - When visiting `Ty::Array(elem, _size)` and size comes from a const generic, substitute the concrete value

    2. In spec_parser.rs, extend name resolution:
       - When resolving identifiers in spec expressions, check if the name matches a const generic parameter
       - If so, emit an SMT constant declaration (DeclareConst name IntSort) and reference it as a named SMT variable
       - Const generic params from `func.generic_params.iter().filter(|p| p.is_const)` are available in scope

    3. In vcgen.rs, wire const generic SMT preamble:
       - At function VCGen entry, for each const generic param, emit `(declare-const N Int)` (or appropriate sort)
       - For integer const generics, add range constraints: `(assert (>= N 0))` for unsigned types
       - At call sites with concrete const args, emit `(assert (= N concrete_value))`

    4. In encode_term.rs, handle const generic references:
       - When encoding a term that references a const generic name, emit the SMT variable reference
       - Array length terms with const generic size use the symbolic N

    5. Extend const_generic_tests.rs with integration tests:
       - Test monomorphization end-to-end
       - Test spec parser const generic resolution
       - Test VCGen with requires(N > 0) and concrete call sites
  </action>
  <verify>
    <automated>cd /Users/alexanderfedin/Projects/hapyy/rust-fv && cargo test -p rust-fv-analysis const_generic -- --nocapture 2>&1 | tail -20 && cargo clippy -p rust-fv-analysis -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <done>Const generic parameters are fully wired: MIR extraction -> IR -> SMT encoding -> monomorphization -> spec parser -> VCGen. A function with #[requires(N > 0)] verifies correctly. Concrete call sites substitute N. All tests pass, clippy clean.</done>
</task>

</tasks>

<verification>
- `cargo test -p rust-fv-analysis` passes (all existing + new tests)
- `cargo test -p rust-fv-driver` passes (MIR converter changes)
- `cargo clippy -p rust-fv-analysis -p rust-fv-driver -- -D warnings` clean
- New Ty variants have sort encoding
- VcKind variants have severity and diagnostic code
</verification>

<success_criteria>
- Ty::ConstGeneric and Ty::Union exist in IR with proper pattern matching
- GenericParam has is_const field for const generic detection
- MIR converter extracts const generic params from rustc
- Monomorphization substitutes const generic values at call sites
- Spec parser resolves const generic names in #[requires]/#[ensures]
- VCGen emits SMT constant declarations for const generics
- All 5 new VcKind variants exist (UnionAccess, DropOrder, PinSafety + any needed)
- Tests demonstrate const generic verification end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/51-core-language-features-i/51-01-SUMMARY.md`
</output>

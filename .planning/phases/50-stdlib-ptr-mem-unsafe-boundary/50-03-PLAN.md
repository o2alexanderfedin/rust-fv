---
phase: 50-stdlib-ptr-mem-unsafe-boundary
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/analysis/src/vcgen.rs
  - crates/driver/src/diagnostics.rs
  - crates/driver/src/callbacks.rs
  - crates/analysis/tests/async_w080_test.rs
autonomous: true
requirements: [COMPL-25]

must_haves:
  truths:
    - "Async function spawning threads produces W080 warning"
    - "Plain async function without thread spawning does NOT produce W080"
    - "Function still verifies under sequential model when W080 fires"
    - "W080 message says 'async multi-threaded execution not modeled -- sequential polling assumed'"
  artifacts:
    - path: "crates/analysis/src/vcgen.rs"
      provides: "W080 async multi-thread detection logic"
      contains: "VcKind::AsyncSequentialModel"
    - path: "crates/driver/src/diagnostics.rs"
      provides: "W080 diagnostic text"
      contains: "W080"
    - path: "crates/analysis/tests/async_w080_test.rs"
      provides: "Tests for W080 detection"
  key_links:
    - from: "crates/analysis/src/vcgen.rs"
      to: "crates/driver/src/diagnostics.rs"
      via: "VcKind::AsyncSequentialModel mapped to W080"
      pattern: "AsyncSequentialModel"
---

<objective>
Add W080 diagnostic warning for async functions that spawn threads, documenting the sequential polling assumption as a known limitation.

Purpose: When a user runs `cargo verify` on an async function that spawns threads, they get a clear warning that multi-threaded execution is not modeled — the verification still succeeds under a sequential polling assumption, but the limitation is explicitly surfaced.

Output: VcKind::AsyncSequentialModel, W080 diagnostic, thread-spawn detection in VCGen, tests.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-stdlib-ptr-mem-unsafe-boundary/50-CONTEXT.md

<interfaces>
<!-- Key types from existing async verification infrastructure -->

From crates/analysis/src/vcgen.rs:
```rust
pub enum VcKind {
    AsyncStateInvariantSuspend, // State invariant at .await suspend
    AsyncStateInvariantResume,  // State invariant at .await resume
    AsyncPostcondition,         // Postcondition at Poll::Ready
    OpaqueCallee,               // V060 — uncontracted callee warning
    // ... add AsyncSequentialModel here
}
```

From crates/analysis/src/ir.rs:
```rust
pub struct Function {
    pub name: String,
    pub is_async: bool,
    pub basic_blocks: Vec<BasicBlock>,
    pub call_sites: Vec<CallSiteInfo>,
    // ...
}

pub struct CallSiteInfo {
    pub callee_name: String,
    pub raw_callee_name: String, // pre-normalization name
    // ...
}
```

From crates/driver/src/diagnostics.rs pattern:
- VcKind -> diagnostic code mapping
- VcKind -> severity mapping (Warning for informational)
- VcKind -> description mapping
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: W080 async multi-thread sequential model warning</name>
  <files>crates/analysis/src/vcgen.rs, crates/driver/src/diagnostics.rs, crates/driver/src/callbacks.rs, crates/analysis/tests/async_w080_test.rs</files>
  <behavior>
    - Test: Async function with tokio::spawn call in call_sites generates VcKind::AsyncSequentialModel VC
    - Test: Async function with std::thread::spawn call generates AsyncSequentialModel VC
    - Test: Async function WITHOUT any spawn call does NOT generate AsyncSequentialModel VC
    - Test: Non-async function with spawn call does NOT generate AsyncSequentialModel VC (only async fns)
    - Test: W080 diagnostic message contains "async multi-threaded execution not modeled -- sequential polling assumed"
    - Test: AsyncSequentialModel VC has Warning severity (informational, not blocking)
    - Test: Function with W080 still produces normal postcondition VCs (verification continues)
  </behavior>
  <action>
    1. In vcgen.rs, add VcKind::AsyncSequentialModel variant — for W080 async sequential model warning.

    2. In vcgen.rs, add generate_async_model_vcs(function) -> Vec<Vc>:
       - Only fires if function.is_async == true
       - Scan function.call_sites for thread-spawning callees by name pattern:
         * "tokio::spawn" or contains "::spawn" with tokio/async-std context
         * "std::thread::spawn"
         * "rayon::spawn" or "rayon::join"
         * "crossbeam::scope" or "crossbeam::spawn"
       - If ANY spawn callee found: emit single VcKind::AsyncSequentialModel VC with BoolLit(true) assertion (always passes — this is a warning, not a proof obligation)
       - If no spawn callees found: emit nothing

    3. Wire generate_async_model_vcs into generate_vcs_with_db (call alongside generate_alignment_vcs etc.)

    4. In diagnostics.rs:
       - Add W080 for AsyncSequentialModel: "async multi-threaded execution not modeled -- sequential polling assumed"
       - Severity: Warning (informational)
       - suggest_fix: "Consider sequential verification only, or split async and threaded verification"
       - Add AsyncSequentialModel to the list of VcKinds that use Warning report_kind (alongside OpaqueCallee, AlignmentSafety, etc.)

    5. In callbacks.rs: Add AsyncSequentialModel => "async_sequential_model" in vc_kind_to_string

    6. Create async_w080_test.rs:
       - Build async Function with tokio::spawn call site, verify AsyncSequentialModel VC generated
       - Build async Function with std::thread::spawn, verify AsyncSequentialModel VC generated
       - Build async Function without spawn, verify NO AsyncSequentialModel VC
       - Build non-async Function with spawn, verify NO AsyncSequentialModel VC
       - Verify the VC assertion is BoolLit(true) (always passes — warning only)
       - Verify W080 diagnostic message text
  </action>
  <verify>
    <automated>cd /Users/alexanderfedin/Projects/hapyy/rust-fv && cargo test -p rust-fv-analysis --test async_w080_test -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>W080 fires for async fns with thread spawning; silent for plain async fns; verification continues under sequential model; diagnostic text matches spec; all tests pass</done>
</task>

</tasks>

<verification>
- `cargo test -p rust-fv-analysis --test async_w080_test` — all tests pass
- `cargo test -p rust-fv-analysis` — full analysis crate tests pass (no regression)
- `cargo clippy -p rust-fv-analysis -p rust-fv-driver` — no warnings
</verification>

<success_criteria>
- Async function with tokio::spawn or std::thread::spawn produces W080 diagnostic
- Plain async function without spawning produces no W080
- W080 is Warning severity, verification continues under sequential model
- Diagnostic message: "async multi-threaded execution not modeled -- sequential polling assumed"
</success_criteria>

<output>
After completion, create `.planning/phases/50-stdlib-ptr-mem-unsafe-boundary/50-03-SUMMARY.md`
</output>

---
phase: 50-stdlib-ptr-mem-unsafe-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/analysis/src/stdlib_contracts/ptr.rs
  - crates/analysis/src/stdlib_contracts/mem.rs
  - crates/analysis/src/stdlib_contracts/mod.rs
  - crates/analysis/src/stdlib_contracts/loader.rs
  - crates/analysis/tests/ptr_contracts_test.rs
  - crates/analysis/tests/mem_contracts_test.rs
autonomous: true
requirements: [COMPL-23, COMPL-24]

must_haves:
  truths:
    - "ptr::copy_nonoverlapping generates overlap and alignment VCs"
    - "ptr::read/write generate alignment and validity VCs"
    - "mem::swap has a_post == b_pre && b_post == a_pre postcondition"
    - "mem::replace returns old value and stores new value"
    - "All ptr/mem contracts auto-load via StdlibContractRegistry"
  artifacts:
    - path: "crates/analysis/src/stdlib_contracts/ptr.rs"
      provides: "Ptr stdlib contracts for read/write/copy/copy_nonoverlapping/swap/replace/drop_in_place/null"
      exports: ["register_ptr_contracts"]
    - path: "crates/analysis/src/stdlib_contracts/mem.rs"
      provides: "Mem stdlib contracts for swap/replace/forget/size_of/align_of"
      exports: ["register_mem_contracts"]
    - path: "crates/analysis/tests/ptr_contracts_test.rs"
      provides: "Unit tests for ptr contract generation"
    - path: "crates/analysis/tests/mem_contracts_test.rs"
      provides: "Unit tests for mem contract generation"
  key_links:
    - from: "crates/analysis/src/stdlib_contracts/loader.rs"
      to: "crates/analysis/src/stdlib_contracts/ptr.rs"
      via: "register_ptr_contracts call in load_default_contracts"
      pattern: "super::ptr::register_ptr_contracts"
    - from: "crates/analysis/src/stdlib_contracts/loader.rs"
      to: "crates/analysis/src/stdlib_contracts/mem.rs"
      via: "register_mem_contracts call in load_default_contracts"
      pattern: "super::mem::register_mem_contracts"
---

<objective>
Create stdlib contracts for std::ptr and std::mem modules with zero-config activation via StdlibContractRegistry.

Purpose: Enable automatic safety VC generation for low-level pointer operations (copy_nonoverlapping overlap checks, alignment checks) and memory operations (swap value exchange, replace semantics) without requiring user-supplied contracts.

Output: Two new stdlib contract modules (ptr.rs, mem.rs) registered in the loader, with comprehensive unit tests.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-stdlib-ptr-mem-unsafe-boundary/50-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs from existing stdlib contract modules -->

From crates/analysis/src/stdlib_contracts/types.rs:
- StdlibContractRegistry (has register_contract method)
- StdlibContract { type_path, method_name, preconditions, postconditions, source }
- ContractSource::Builtin

From crates/analysis/src/stdlib_contracts/vec.rs (pattern to follow):
- pub fn register_vec_contracts(registry: &mut StdlibContractRegistry) { ... }
- Each contract built with StdlibContract { type_path: "std::vec::Vec", method_name: "push", ... }

From crates/analysis/src/stdlib_contracts/loader.rs:
- load_default_contracts() calls super::vec::register_vec_contracts(&mut registry), etc.

From crates/analysis/src/ir.rs:
- Term::App(String, Vec<Term>) for SMT applications
- Term::Const(String, Sort) for SMT constants
- Term::BoolLit(bool) for boolean literals
- Sort::BitVec(u32) for bitvector sorts (pointers are BitVec(64))
- Sort::Int for integer sort

From crates/analysis/src/vcgen.rs:
- VcKind::MemorySafety for memory safety VCs
- VcKind::AlignmentSafety for alignment VCs
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ptr.rs and mem.rs stdlib contract modules</name>
  <files>crates/analysis/src/stdlib_contracts/ptr.rs, crates/analysis/src/stdlib_contracts/mem.rs, crates/analysis/src/stdlib_contracts/mod.rs, crates/analysis/src/stdlib_contracts/loader.rs, crates/analysis/tests/ptr_contracts_test.rs, crates/analysis/tests/mem_contracts_test.rs</files>
  <behavior>
    - Test: register_ptr_contracts registers contracts for ptr::read, ptr::write, ptr::copy, ptr::copy_nonoverlapping, ptr::swap, ptr::replace, ptr::drop_in_place, ptr::null, ptr::null_mut
    - Test: ptr::copy_nonoverlapping contract has overlap precondition using address range check (bitvector arithmetic: assert !(src <= dst+count*size && dst <= src+count*size))
    - Test: ptr::copy_nonoverlapping contract has alignment precondition for both src and dst
    - Test: ptr::read/write contracts have alignment preconditions
    - Test: register_mem_contracts registers contracts for mem::swap, mem::replace, mem::forget, mem::size_of, mem::align_of
    - Test: mem::swap postcondition asserts a_post == b_pre && b_post == a_pre
    - Test: mem::replace postcondition asserts result == old_val && place_post == new_val
    - Test: mem::forget modeled as no-op (skip destructor, postcondition is true)
    - Test: All contracts load via load_default_contracts (registry not empty after loading)
  </behavior>
  <action>
    Create crates/analysis/src/stdlib_contracts/ptr.rs:
    - pub fn register_ptr_contracts(registry: &mut StdlibContractRegistry)
    - Register contracts for type_path "std::ptr" with methods: read, write, copy, copy_nonoverlapping, swap, replace, drop_in_place, null, null_mut
    - copy_nonoverlapping precondition: overlap check using bitvector address range — Term::App("not", [Term::App("and", [src <= dst+count*size, dst <= src+count*size])]) where size is element size
    - copy_nonoverlapping precondition: alignment check — Term::App("=", [Term::App("bvsmod", [src_addr, align]), bv_zero])
    - read/write preconditions: alignment check on the pointer argument
    - ptr::null/null_mut: postcondition result == 0 (null pointer)
    - Follow exact pattern from vec.rs for StdlibContract construction

    Create crates/analysis/src/stdlib_contracts/mem.rs:
    - pub fn register_mem_contracts(registry: &mut StdlibContractRegistry)
    - Register contracts for type_path "std::mem" with methods: swap, replace, forget, size_of, align_of
    - swap postcondition: a_post == b_pre && b_post == a_pre (uses old() spec function pattern)
    - replace postcondition: result == old(place) && place_post == new_val
    - forget: postcondition BoolLit(true) — modeled as no-op skipping destructor
    - size_of/align_of: postcondition result > 0 (positive integer constants)

    Update mod.rs: Add `pub mod ptr;` and `pub mod mem;` declarations

    Update loader.rs: Add `super::ptr::register_ptr_contracts(&mut registry);` and `super::mem::register_mem_contracts(&mut registry);` calls in load_default_contracts()

    Create tests following existing test patterns (see ptr_contracts_test.rs pattern from nonnull_test.rs):
    - Verify contracts are registered with correct type_paths and method names
    - Verify preconditions contain expected SMT terms (overlap check, alignment check)
    - Verify postconditions contain expected SMT terms (swap exchange, replace semantics)
    - Verify load_default_contracts includes ptr and mem contracts
  </action>
  <verify>
    <automated>cd /Users/alexanderfedin/Projects/hapyy/rust-fv && cargo test -p rust-fv-analysis --test ptr_contracts_test --test mem_contracts_test -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>ptr.rs registers 9 ptr contracts with overlap/alignment preconditions; mem.rs registers 5 mem contracts with swap/replace postconditions; loader wires both; all tests pass</done>
</task>

</tasks>

<verification>
- `cargo test -p rust-fv-analysis --test ptr_contracts_test` — all ptr contract tests pass
- `cargo test -p rust-fv-analysis --test mem_contracts_test` — all mem contract tests pass
- `cargo test -p rust-fv-analysis stdlib_contracts` — all stdlib contract tests including loader pass
- `cargo clippy -p rust-fv-analysis` — no warnings
</verification>

<success_criteria>
- ptr::copy_nonoverlapping has overlap and alignment preconditions in its registered contract
- mem::swap has value exchange postcondition (a_post == b_pre, b_post == a_pre)
- All contracts auto-load through StdlibContractRegistry (zero-config)
- Test coverage for all contract registrations and key SMT term shapes
</success_criteria>

<output>
After completion, create `.planning/phases/50-stdlib-ptr-mem-unsafe-boundary/50-01-SUMMARY.md`
</output>

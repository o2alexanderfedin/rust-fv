---
phase: 46-smt-datatype-foundations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/analysis/src/vcgen.rs
  - crates/analysis/src/encode_sort.rs
  - crates/analysis/tests/e2e_verification.rs
autonomous: true
requirements: [COMPL-01, COMPL-05]

must_haves:
  truths:
    - "Struct types use SMT declare-datatype constructors and selectors instead of uninterpreted sorts"
    - "Enum types use SMT declare-datatype with testers (is-Variant) instead of uninterpreted discriminant functions"
    - "Functional update works for nested field projections (s.inner.field = val)"
    - "Functional update works for enum variant fields via Downcast projection"
    - "All rvalue types are supported on projected place LHS (not just Use)"
  artifacts:
    - path: "crates/analysis/src/vcgen.rs"
      provides: "Enhanced functional update with nested projections, Downcast, all rvalue types; discriminant encoding via native testers"
      contains: "Downcast"
    - path: "crates/analysis/src/encode_sort.rs"
      provides: "Uninterpreted sort fallback elimination for struct/enum types"
      contains: "collect_datatype_declarations"
    - path: "crates/analysis/tests/e2e_verification.rs"
      provides: "E2E tests for struct datatype encoding and functional update hardening"
      contains: "declare_datatype"
  key_links:
    - from: "crates/analysis/src/vcgen.rs"
      to: "crates/analysis/src/encode_sort.rs"
      via: "collect_datatype_declarations called in generate_vcs_with_db"
      pattern: "collect_datatype_declarations"
    - from: "crates/analysis/src/vcgen.rs"
      to: "crates/smtlib/src/term.rs"
      via: "mk-StructName constructor terms for functional update"
      pattern: "mk-"
---

<objective>
Complete struct/enum SMT datatype wiring and harden functional update for all rvalue types, nested projections, and enum variant fields.

Purpose: COMPL-01 ensures structs/enums are fully first-class SMT datatypes (not uninterpreted approximations), enabling Z3 to reason about field values natively. COMPL-05 ensures functional updates work for all rvalue types on projected places, nested struct fields, and enum variant fields — not just single-level `Rvalue::Use`.
Output: Structs and enums verified via native SMT datatype reasoning; functional update handles nested projections and all rvalue types.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-smt-datatype-foundations/46-CONTEXT.md

@crates/analysis/src/vcgen.rs
@crates/analysis/src/encode_sort.rs
@crates/analysis/src/encode_term.rs
@crates/smtlib/src/term.rs
@crates/smtlib/src/command.rs
@crates/smtlib/src/sort.rs

<interfaces>
<!-- From crates/analysis/src/vcgen.rs (functional update, lines 1684-1713) -->
Current single-level functional update:
- Only handles `place.projections.len() == 1 && Projection::Field(idx)`
- Only handles `Ty::Struct(name, fields)` base type
- Builds `Term::App(format!("mk-{name}"), field_terms)` with selector for unchanged fields
- Fallback at line 1718: `_ => return None` for complex rvalues on projected places

encode_rvalue_for_assignment(rvalue, func, dest) -> Option<Term>:
- Supports: Use, BinaryOp, CheckedBinaryOp, UnaryOp, Cast, Ref, Len, Discriminant, Aggregate
- Does NOT handle: Repeat (returns None — handled by Plan 01)

<!-- From crates/analysis/src/ir.rs -->
pub enum Projection { Field(usize), Index(Operand), Downcast(usize), Deref }
pub enum Ty { Struct(String, Vec<(String, Ty)>), Enum(String, Vec<EnumVariant>), ... }
pub struct EnumVariant { pub name: String, pub fields: Vec<(String, Ty)> }

<!-- From crates/analysis/src/encode_sort.rs -->
pub fn collect_datatype_declarations(func: &Function) -> Vec<Command>
  - Already handles Struct, Tuple, Enum, Closure types
  - Produces DeclareDatatype commands with constructors/selectors
pub fn encode_type(ty: &Ty) -> Sort
  - Maps Ty::Struct/Enum -> Sort::Datatype(name)
  - Some paths may still fall back to Sort::Uninterpreted

<!-- From crates/smtlib/src/term.rs -->
Term::App(String, Vec<Term>) — used for mk-StructName constructors and StructName-field selectors

<!-- From crates/analysis/src/vcgen.rs (discriminant, line 1780-1788) -->
Current discriminant encoding uses uninterpreted `Term::App("discriminant-{local}", ...)`:
  let disc_fn = format!("discriminant-{}", disc_place.local);
  Term::App(disc_fn, vec![Term::Const(disc_place.local.clone())])
Per CONTEXT.md decision: should use Z3 built-in `((_ is mk-VariantName) expr)` testers from declare-datatype
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Datatype wiring completion and discriminant testers (COMPL-01)</name>
  <files>crates/analysis/src/vcgen.rs, crates/analysis/src/encode_sort.rs, crates/analysis/tests/e2e_verification.rs</files>
  <behavior>
    - Test: A struct `Point { x: i32, y: i32 }` used in a function produces DeclareDatatype in the SMT script (already works via collect_datatype_declarations)
    - Test: Field access `point.x` encodes as `(Point-x point)` selector application (already works)
    - Test: Aggregate initialization `Point { x: 1, y: 2 }` encodes as `(mk-Point 1 2)` constructor (already works via encode_aggregate_with_type)
    - Test: An enum `Option_i32` discriminant comparison uses `((_ is mk-Some) val)` tester instead of uninterpreted `discriminant-_X` function
    - Test: E2E: A function creating a struct, reading a field, and asserting field equality verifies as UNSAT via native datatype reasoning (no uninterpreted sort fallback)
    - Test: E2E: A function matching on an enum variant uses declare-datatype testers and verifies correctly
    - Test: No remaining `Sort::Uninterpreted` fallback for known struct/enum types in encode_sort.rs
  </behavior>
  <action>
1. **Audit and eliminate uninterpreted sort fallbacks for struct/enum types** in `encode_sort.rs`:
   - Search for any `Sort::Uninterpreted` usage in `encode_type()` that applies to `Ty::Struct` or `Ty::Enum`
   - Ensure `Ty::Struct(name, _)` always maps to `Sort::Datatype(name.clone())`
   - Ensure `Ty::Enum(name, _)` always maps to `Sort::Datatype(name.clone())`
   - If there are edge cases where type info is missing, emit a warning but still use Datatype sort

2. **Replace discriminant encoding with native testers** in vcgen.rs:
   - In `encode_assignment()`, the `Rvalue::Discriminant` arm currently produces `Term::App("discriminant-{local}", ...)` (an uninterpreted function)
   - Per CONTEXT.md decision, this should use the Z3 built-in tester syntax. However, the discriminant is stored as an integer that SwitchInt compares against variant indices. The existing approach works correctly with SwitchInt.
   - Add a `Term::IsTester(String, Box<Term>)` variant to term.rs (or use `Term::App("(_ is mk-VariantName)", ...)`) for the tester syntax `((_ is mk-VariantName) expr)`. This is used at SwitchInt comparison sites.
   - Update the SwitchInt encoding in vcgen.rs to use testers when the discriminant source is an enum: instead of comparing `discriminant-_X(val) == 0`, emit `((_ is mk-None) val)` for variant 0 and `((_ is mk-Some) val)` for variant 1.
   - If the SwitchInt path already works correctly with the uninterpreted function approach and testers would require significant refactoring of the SwitchInt logic, document why the current approach is sound and defer native testers. The key goal is that struct/enum types are DeclareDatatype — the tester optimization is secondary.

3. **Add E2E tests** in e2e_verification.rs:
   - `e2e_struct_datatype_field_reasoning`: Create a function with `Point { x: 1, y: 2 }`, assert `point.x == 1` — verify UNSAT
   - `e2e_enum_datatype_match`: Create a function matching on `Option_i32`, assert correct variant handling — verify UNSAT

4. Run `cargo test -p rust-fv-analysis` to ensure no regressions.
  </action>
  <verify>
    <automated>cd /Users/alexanderfedin/Projects/hapyy/rust-fv && cargo test -p rust-fv-analysis e2e_struct_datatype -- --nocapture 2>&1 | tail -20 && cargo test -p rust-fv-analysis e2e_enum_datatype -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>Struct/enum types produce DeclareDatatype commands in SMT scripts. Field access uses selectors, initialization uses constructors. E2E tests confirm Z3 reasons about struct field values correctly (not treating them as opaque uninterpreted sorts). No Sort::Uninterpreted fallback for struct/enum types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Functional update hardening for nested projections and all rvalue types (COMPL-05)</name>
  <files>crates/analysis/src/vcgen.rs, crates/analysis/tests/e2e_verification.rs</files>
  <behavior>
    - Test: Nested field projection `outer.inner.field = val` produces chained mk-Inner inside mk-Outer constructor
    - Test: Enum variant field mutation via Downcast projection `opt.Some.0 = val` produces mk-Some constructor with updated field
    - Test: All rvalue types (BinaryOp, CheckedBinaryOp, UnaryOp, Cast, Ref, Len, Discriminant) work on projected LHS places (not just Use)
    - Test: E2E: A function with nested struct update `s.inner.x = s.inner.x + 1` and postcondition verifies correctly
    - Test: E2E: A function with enum variant field update verifies correctly
  </behavior>
  <action>
1. **Extend functional update for nested projections** in `encode_assignment()`:
   - Currently handles only `place.projections.len() == 1` with `Projection::Field`
   - Add handling for multi-level Field projections (e.g., `[Field(0), Field(1)]` for `s.inner.field`)
   - For nested projection `s.inner.field = val`:
     - Inner update: `(mk-Inner (Inner-other inner) new_val)` — update the innermost struct
     - Outer update: `(mk-Outer updated_inner (Outer-other s))` — wrap in outer constructor
   - Implementation: Walk projections from innermost to outermost, building chained constructor terms
   - Need to resolve type at each projection level: use `find_local_type` for base, then walk struct field types

2. **Add Downcast projection support** for enum variant fields:
   - Projection sequence like `[Downcast(variant_idx), Field(field_idx)]` means "access field of enum variant"
   - For mutation: build `(mk-VariantName updated_field (VariantName-other_field val))` constructor
   - The base variable keeps its type as the enum, so the result is the enum constructor for that variant

3. **Extend the projected-place fallback** (vcgen.rs line ~1714-1720):
   - Currently: `_ => return None` for non-Use rvalues on projected places
   - Change to: call `encode_rvalue_for_assignment(rvalue, func, place)` for ALL rvalue types, not just Use
   - The functional update path (line 1689-1712) already uses `encode_rvalue_for_assignment` which supports all rvalue types — the issue is the fallback path at line 1718
   - Update the fallback to use `encode_rvalue_for_assignment` instead of only handling Use

4. **Add unit tests** in vcgen.rs tests:
   - `encode_assignment_nested_field_projection`: Two-level field projection produces nested mk- constructors
   - `encode_assignment_downcast_field`: Downcast + Field produces variant constructor
   - `encode_assignment_projected_binop`: BinaryOp rvalue on a projected place produces correct functional update

5. **Add E2E tests** in e2e_verification.rs:
   - `e2e_nested_struct_update`: Nested struct field mutation verifies correctly
   - `e2e_functional_update_binop`: `s.x = s.x + 1` with postcondition verifies

6. Run full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd /Users/alexanderfedin/Projects/hapyy/rust-fv && cargo test -p rust-fv-analysis encode_assignment_nested -- --nocapture 2>&1 | tail -20 && cargo test -p rust-fv-analysis encode_assignment_downcast -- --nocapture 2>&1 | tail -20 && cargo test -p rust-fv-analysis e2e_nested_struct -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>Nested field projections (s.inner.field) produce chained mk-Inner/mk-Outer constructors. Downcast projections for enum variant fields produce variant constructors. All rvalue types (BinaryOp, Cast, etc.) work on projected LHS places. E2E tests confirm nested struct updates and variant field mutations verify correctly. The `_ => return None` fallback at line 1718 is eliminated for supported rvalue types.</done>
</task>

</tasks>

<verification>
1. `cargo test -p rust-fv-analysis` — all tests pass including new datatype and functional update tests
2. `cargo clippy --all-targets` — no warnings
3. Grep for `Sort::Uninterpreted` in encode_sort.rs — no remaining fallbacks for struct/enum types
4. Grep for `return None` in encode_assignment for Repeat — eliminated (handled by Plan 01)
5. Grep for `_ => return None` at the projected-place fallback — eliminated or reduced to only truly unsupported cases
</verification>

<success_criteria>
- Struct/enum types are first-class SMT datatypes with constructors, selectors, and testers (COMPL-01 complete)
- Functional update works for nested projections, Downcast variant fields, and all rvalue types (COMPL-05 complete)
- All existing tests continue to pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/46-smt-datatype-foundations/46-02-SUMMARY.md`
</output>

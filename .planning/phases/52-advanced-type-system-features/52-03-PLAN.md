---
phase: 52-advanced-type-system-features
plan: 03
type: execute
wave: 2
depends_on: [52-02]
files_modified:
  - crates/analysis/src/vcgen.rs
  - crates/analysis/src/trait_analysis.rs
  - crates/driver/src/callbacks.rs
  - crates/driver/src/diagnostics.rs
  - crates/analysis/tests/gat_tests.rs
  - crates/analysis/tests/trait_upcast_tests.rs
autonomous: true
requirements: [LANG-08, LANG-09]

must_haves:
  truths:
    - "GAT type Item<'a> with where Self: 'a generates well-formedness VC at use sites"
    - "Violating GAT instantiation produces Z3 SAT counterexample"
    - "Casting dyn SubTrait to dyn SuperTrait generates vtable compatibility VC"
    - "SuperTrait contracts preserved and checked at upcast call sites"
    - "Upcast chain preserves contracts transitively"
  artifacts:
    - path: "crates/analysis/src/vcgen.rs"
      provides: "GAT WellFormedness VCs (V130) + TraitUpcasting VCs (V140)"
      contains: "WellFormedness"
    - path: "crates/analysis/src/trait_analysis.rs"
      provides: "Upcast compatibility checks + GAT detection helpers"
      contains: "upcast\\|gat"
    - path: "crates/driver/src/diagnostics.rs"
      provides: "V130 WellFormedness + V140 TraitUpcasting diagnostic codes"
      contains: "V130"
    - path: "crates/analysis/tests/gat_tests.rs"
      provides: "GAT well-formedness integration tests"
      min_lines: 50
    - path: "crates/analysis/tests/trait_upcast_tests.rs"
      provides: "Trait upcasting integration tests"
      min_lines: 50
  key_links:
    - from: "crates/analysis/src/vcgen.rs"
      to: "HRTB lifetime encoding"
      via: "GAT well-formedness reuses lifetime-as-Int pattern from Phase 51 LANG-02"
      pattern: "lifetime.*Int\\|well_formedness"
    - from: "crates/analysis/src/trait_analysis.rs"
      to: "TraitDef.super_traits"
      via: "Upcast chain resolution via existing super_traits field"
      pattern: "super_traits"
    - from: "crates/analysis/src/vcgen.rs"
      to: "behavioral subtyping"
      via: "Upcast contracts preserved via existing Liskov infrastructure"
      pattern: "subtyping\\|upcast"
---

<objective>
Generate well-formedness constraints for Generic Associated Types (GATs) at use sites using lifetime outlives assertions, and model trait upcasting with vtable compatibility VCs that preserve supertype contracts at call sites.

Purpose: LANG-08 requires GAT `where Self: 'a` bounds to produce well-formedness VCs at instantiation sites. LANG-09 requires trait upcasting (`dyn SubTrait` to `dyn SuperTrait`) to preserve contracts. Both extend the verification of trait-related type system features.

Output: WellFormedness VcKind (V130), TraitUpcasting VcKind (V140), GAT use-site VCs, upcast vtable compatibility VCs, integration tests.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-advanced-type-system-features/52-CONTEXT.md
@.planning/phases/51-core-language-features-i/51-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From crates/analysis/src/vcgen.rs:
```rust
pub enum VcKind {
    // ... existing variants through PinSafety
    PanicSafety,      // V120, added by plan 52-02
    // ADD: WellFormedness   -- V130, GAT constraint violations
    // ADD: TraitUpcasting   -- V140, vtable compatibility
}
```

NOTE: V130 diagnostic code is currently used for UnionAccess. Executor must check existing V130 usage and pick the next available code if V130 is taken. The CONTEXT.md specified V130 for GATs but Phase 51 used V130 for UnionAccess. Use V160 for WellFormedness and V170 for TraitUpcasting to avoid conflicts.

From crates/analysis/src/ir.rs:
```rust
pub struct TraitDef {
    pub name: String,
    pub methods: Vec<TraitMethod>,
    pub is_sealed: bool,
    pub super_traits: Vec<String>,  // Already tracks super-trait hierarchy
}
```

From crates/analysis/src/trait_analysis.rs:
```rust
pub struct TraitDatabase {
    traits: HashMap<String, TraitDef>,
    impls: HashMap<String, Vec<TraitImpl>>,
    negative_impls: HashMap<String, Vec<String>>,  // Added by plan 52-02
}
```

From Phase 51 HRTB encoding:
```rust
// Lifetimes encoded as Int SMT constants with non-negative constraints
// Pattern: declare-const lifetime_a Int, assert (>= lifetime_a 0)
// GAT well-formedness reuses this: assert (>= lifetime_of_self lifetime_a)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAT well-formedness VCs with lifetime outlives assertions</name>
  <files>crates/analysis/src/vcgen.rs, crates/driver/src/diagnostics.rs, crates/driver/src/callbacks.rs, crates/analysis/tests/gat_tests.rs</files>
  <behavior>
    - Test 1: GAT `type Item<'a> where Self: 'a` generates WellFormedness VC at use site
    - Test 2: Valid GAT instantiation (Self outlives 'a) produces UNSAT (verified)
    - Test 3: Invalid GAT instantiation produces SAT (counterexample)
    - Test 4: GAT with multiple lifetime parameters generates VC for each where clause
    - Test 5: GAT interacting with HRTB `for<'a>` composes correctly
    - Test 6: Function without GATs generates no WellFormedness VCs
  </behavior>
  <action>
1. Add `WellFormedness` variant to `VcKind` enum in vcgen.rs. Check existing diagnostic codes -- V130 is used by UnionAccess (Phase 51). Use V160 for WellFormedness to avoid collision. Update diagnostics.rs with V160 code and Warning severity. Update callbacks.rs with vc_kind_to_string mapping.

2. Create `generate_gat_well_formedness_vcs(func: &Function, db: &TraitDatabase) -> Vec<VerificationCondition>`:
   - Scan function body for uses of associated types that have generic parameters (GATs). Detection: look for type references that match `TraitName::AssocType<'a>` patterns in local types, return types, or generic bounds.
   - For each GAT use site, extract the where clauses from the GAT definition. For `where Self: 'a`, generate an SMT assertion: `(assert (not (>= lifetime_self lifetime_a)))` -- if SAT, the Self doesn't outlive 'a (violation).
   - Lifetime encoding reuses Phase 51 HRTB pattern: lifetimes are Int SMT constants with non-negative constraints.
   - For GAT parameter substitution: when `Item<'a>` is instantiated with a concrete lifetime, substitute and check the where clause holds.

3. To detect GATs from the trait database, add a helper method to TraitDatabase or TraitDef:
   - `get_gat_bounds(trait_name: &str, assoc_type: &str) -> Vec<(String, String)>` -- returns list of (lifetime_param, outlives_constraint) pairs for the GAT.
   - GAT definitions are detected in callbacks.rs during HIR traversal: associated types with generic parameters. Store GAT where-clauses in a new field on TraitDef or as a separate map in TraitDatabase.

4. Wire `generate_gat_well_formedness_vcs` into `generate_vcs_with_db`.

5. Create `crates/analysis/tests/gat_tests.rs` with 6+ tests per behavior list.

6. Run clippy and test suite.
  </action>
  <verify>
    <automated>cd /Users/alexanderfedin/Projects/hapyy/rust-fv && cargo test -p rust-fv-analysis --test gat_tests 2>&1 | tail -10</automated>
  </verify>
  <done>WellFormedness VcKind with V160 diagnostic, GAT where clauses generate lifetime outlives VCs at use sites, HRTB composition works, 6+ integration tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Trait upcasting vtable compatibility VCs + contract preservation</name>
  <files>crates/analysis/src/vcgen.rs, crates/analysis/src/trait_analysis.rs, crates/analysis/tests/trait_upcast_tests.rs</files>
  <behavior>
    - Test 1: Casting dyn SubTrait to dyn SuperTrait generates TraitUpcasting VC
    - Test 2: SuperTrait method contracts preserved at upcast call sites
    - Test 3: Upcast chain A: B: C preserves contracts transitively
    - Test 4: Casting to non-supertrait generates SAT VC (violation)
    - Test 5: Upcast with no contracts generates compatibility-only VC (no contract VCs)
    - Test 6: No upcasting in function generates no TraitUpcasting VCs
  </behavior>
  <action>
1. Add `TraitUpcasting` variant to `VcKind` enum in vcgen.rs. Use V170 diagnostic code (next available). Severity: Warning. Update diagnostics.rs and callbacks.rs.

2. Add upcast compatibility helper to trait_analysis.rs:
   - `is_supertrait(db: &TraitDatabase, sub: &str, super_: &str) -> bool` -- checks if super_ is a supertrait of sub by walking the `super_traits` field transitively
   - `get_supertrait_chain(db: &TraitDatabase, sub: &str, super_: &str) -> Option<Vec<String>>` -- returns the chain of traits from sub to super_ if valid

3. Create `generate_trait_upcast_vcs(func: &Function, db: &TraitDatabase) -> Vec<VerificationCondition>`:
   - Walk basic blocks looking for cast operations (assignments with cast-like rvalues) that convert between TraitObject types. Detection: look for `Rvalue::Cast` or assignments where the source type is `Ty::TraitObject(sub)` and destination is `Ty::TraitObject(super_)`.
   - For each upcast:
     a. **Vtable compatibility VC**: Assert that `is_supertrait(sub, super_)` holds. Generate `(assert (not is_supertrait))` -- SAT means invalid upcast. VcKind::TraitUpcasting.
     b. **Contract preservation**: For each method on the SuperTrait that has contracts, the contracts are already verified via behavioral subtyping (Phase 38 Liskov checks). No new VC needed -- just verify the subtyping relationship exists. If the impl doesn't exist in the TraitDatabase, generate a warning VC.

4. Wire `generate_trait_upcast_vcs` into `generate_vcs_with_db`.

5. Create `crates/analysis/tests/trait_upcast_tests.rs` with 6+ tests:
   - Valid upcast with contract preservation
   - Invalid upcast (non-supertrait) producing SAT
   - Transitive upcast chain
   - Upcast without contracts
   - No upcast function generates no VCs

6. Run clippy and full test suite.
  </action>
  <verify>
    <automated>cd /Users/alexanderfedin/Projects/hapyy/rust-fv && cargo test -p rust-fv-analysis --test trait_upcast_tests 2>&1 | tail -10</automated>
  </verify>
  <done>TraitUpcasting VcKind with V170 diagnostic, upcast vtable compatibility VCs, supertrait chain validation, contract preservation via behavioral subtyping, 6+ integration tests pass</done>
</task>

</tasks>

<verification>
- `cargo test -p rust-fv-analysis -p rust-fv-driver` -- all existing tests pass
- `cargo test -p rust-fv-analysis --test gat_tests` -- GAT well-formedness tests pass
- `cargo test -p rust-fv-analysis --test trait_upcast_tests` -- trait upcasting tests pass
- `cargo clippy --all-targets -p rust-fv-analysis -p rust-fv-driver` -- no warnings
- VcKind::WellFormedness with V160, VcKind::TraitUpcasting with V170 in vcgen.rs
- TraitDatabase has GAT bound query methods and supertrait chain helpers
</verification>

<success_criteria>
- GAT `where Self: 'a` bounds generate well-formedness VCs at instantiation sites using lifetime-as-Int encoding
- Invalid GAT instantiations produce SAT counterexamples
- Trait upcasting generates vtable compatibility VCs
- SuperTrait contracts preserved through upcast chain
- All tests pass, clippy clean
</success_criteria>

<output>
After completion, create `.planning/phases/52-advanced-type-system-features/52-03-SUMMARY.md`
</output>

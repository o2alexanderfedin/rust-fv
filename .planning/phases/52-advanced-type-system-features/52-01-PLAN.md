---
phase: 52-advanced-type-system-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/analysis/src/ir.rs
  - crates/analysis/src/encode_sort.rs
  - crates/analysis/src/monomorphize.rs
  - crates/analysis/src/behavioral_subtyping.rs
  - crates/driver/src/mir_converter.rs
  - crates/driver/src/callbacks.rs
  - crates/driver/src/cex_render.rs
  - crates/analysis/tests/impl_trait_tests.rs
autonomous: true
requirements: [LANG-07]

must_haves:
  truths:
    - "A function returning impl Trait resolves to concrete type for verification"
    - "RPITIT contracts are inferred from trait specs via behavioral subtyping"
    - "Unresolvable opaque types fall back to Ty::Opaque with trait bound axioms"
  artifacts:
    - path: "crates/analysis/src/ir.rs"
      provides: "Ty::Opaque(String, Vec<TraitBound>) variant"
      contains: "Opaque"
    - path: "crates/analysis/src/encode_sort.rs"
      provides: "SMT sort encoding for Ty::Opaque as uninterpreted sort"
      contains: "Opaque"
    - path: "crates/driver/src/mir_converter.rs"
      provides: "Opaque type resolution from MIR return types"
      contains: "opaque"
    - path: "crates/analysis/tests/impl_trait_tests.rs"
      provides: "Integration tests for impl Trait resolution and RPITIT"
      min_lines: 50
  key_links:
    - from: "crates/driver/src/mir_converter.rs"
      to: "crates/analysis/src/ir.rs"
      via: "Ty::Opaque construction for unresolved opaque types"
      pattern: "Ty::Opaque"
    - from: "crates/analysis/src/behavioral_subtyping.rs"
      to: "trait specs"
      via: "RPITIT postcondition verification against trait-level ensures"
      pattern: "rpitit\\|opaque"
---

<objective>
Resolve `impl Trait` return types to concrete types for verification, add `Ty::Opaque` fallback for unresolvable cross-crate opaque types, and wire RPITIT contract verification through existing behavioral subtyping infrastructure.

Purpose: LANG-07 requires that functions returning `impl Iterator<Item=i32>` with postconditions can be verified by resolving the opaque type to its concrete type from MIR, and that RPITIT contracts in trait impls are checked via Liskov behavioral subtyping.

Output: Ty::Opaque IR variant, MIR opaque type resolver, Opaque sort encoding, RPITIT behavioral subtyping wiring, integration tests.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-advanced-type-system-features/52-CONTEXT.md
@.planning/phases/51-core-language-features-i/51-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From crates/analysis/src/ir.rs:
```rust
pub enum Ty {
    Bool, Int(IntTy), Uint(UintTy), Float(FloatTy), Char, Unit, Never,
    Tuple(Vec<Ty>), Array(Box<Ty>, usize), Slice(Box<Ty>),
    Ref(Box<Ty>, Mutability), RawPtr(Box<Ty>, Mutability),
    NonNull(Box<Ty>), Struct(String, Vec<(String, Ty)>),
    Enum(String, Vec<(String, Vec<Ty>)>), Named(String),
    SpecInt, SpecNat, Generic(String),
    Closure(Box<ClosureInfo>), TraitObject(String),
    ConstGeneric(String, Box<Ty>), Union(String, Vec<(String, Ty)>),
    // ADD: Opaque(String, Vec<TraitBound>)
}
```

From crates/analysis/src/encode_sort.rs:
```rust
pub fn encode_type(ty: &Ty) -> Sort {
    // Match each Ty variant to SMT sort
    // ADD: Ty::Opaque -> Sort::Uninterpreted(name)
}
```

From crates/analysis/src/behavioral_subtyping.rs:
```rust
pub fn generate_subtyping_vcs(
    trait_def: &TraitDef,
    trait_impl: &TraitImpl,
    db: &TraitDatabase,
) -> Vec<SubtypingVc>;
```

From crates/driver/src/mir_converter.rs:
```rust
pub fn convert_mir(...) -> Function {
    // Extract MIR return type
    // ADD: resolve opaque return types to concrete types
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ty::Opaque IR variant + MIR opaque type resolution + SMT encoding</name>
  <files>crates/analysis/src/ir.rs, crates/analysis/src/encode_sort.rs, crates/analysis/src/monomorphize.rs, crates/driver/src/mir_converter.rs, crates/driver/src/callbacks.rs, crates/driver/src/cex_render.rs</files>
  <action>
1. Add `Ty::Opaque(String, Vec<String>)` variant to the Ty enum in ir.rs. The String is the opaque type name (e.g., "impl Iterator"), the Vec<String> is the list of trait bounds (e.g., ["Iterator", "Item=i32"]). Add a `TraitBound` struct if needed or use simple strings for trait names. Update all match arms on Ty across the codebase (follow the pattern from Phase 51 where Ty::Union/ConstGeneric were added -- use grep to find all `match.*ty` in ir.rs, encode_sort.rs, monomorphize.rs, trait_analysis.rs, etc.).

2. In mir_converter.rs, add opaque type resolution in the return type conversion path. When the MIR return type is `ty::Alias(ty::Opaque, ...)`, attempt to resolve it to the concrete hidden type using `tcx.type_of(def_id).instantiate_identity()`. If resolution succeeds, convert the concrete type as usual. If resolution fails (cross-crate opaque type), create `Ty::Opaque(name, trait_bounds)` extracting bounds from the opaque type's predicates.

3. In encode_sort.rs, add encoding for `Ty::Opaque(name, _)` as `Sort::Uninterpreted(name)` -- an uninterpreted sort is sound (any property proven holds for all possible implementations) but incomplete (cannot reason about specific behavior). This matches project soundness philosophy.

4. In monomorphize.rs, add `Ty::Opaque` to `substitute_ty` -- opaque types are not substituted during monomorphization (they are already concrete or intentionally opaque).

5. In cex_render.rs, add `Ty::Opaque(name, _)` to `ty_name_string` returning the opaque type name.

6. In callbacks.rs, add `Ty::Opaque` to any `vc_kind_to_string` or diagnostic formatting that matches on Ty variants.

7. Run `cargo clippy --all-targets -p rust-fv-analysis -p rust-fv-driver` and `cargo test -p rust-fv-analysis -p rust-fv-driver` to ensure no regressions.
  </action>
  <verify>
    <automated>cd /Users/alexanderfedin/Projects/hapyy/rust-fv && cargo test -p rust-fv-analysis -p rust-fv-driver 2>&1 | tail -5</automated>
  </verify>
  <done>Ty::Opaque variant added to IR, MIR opaque type resolution in mir_converter, SMT encoding as uninterpreted sort, all existing tests pass, clippy clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RPITIT behavioral subtyping wiring + trait bound axioms + integration tests</name>
  <files>crates/analysis/src/behavioral_subtyping.rs, crates/analysis/src/vcgen.rs, crates/analysis/tests/impl_trait_tests.rs</files>
  <behavior>
    - Test 1: Function returning concrete type resolved from impl Trait has postcondition verified
    - Test 2: Ty::Opaque with Iterator trait bound generates trait bound SMT axiom
    - Test 3: RPITIT impl with ensures clause verified via behavioral subtyping (Liskov check)
    - Test 4: Opaque type that cannot be resolved falls back to Ty::Opaque with uninterpreted sort
    - Test 5: Auto-trait bounds (Send/Sync) NOT leaked from opaque types unless explicit
  </behavior>
  <action>
1. In behavioral_subtyping.rs, extend `generate_subtyping_vcs` to handle RPITIT cases: when a trait method returns an opaque type (`impl Trait` in trait definition) and the impl returns a concrete type, generate a Liskov subtyping VC asserting the concrete return type satisfies the trait's postcondition. This piggybacks on existing Liskov infrastructure -- the key addition is detecting when the trait method's return type is opaque and the impl's return type is concrete.

2. In vcgen.rs, when generating trait bound axioms (in the preamble or via `trait_bounds_as_smt_assumptions` if that exists), add axiom generation for `Ty::Opaque` types. For each trait bound on the opaque type, assert the trait's contracts as SMT assumptions. For example, `impl Iterator<Item=i32>` generates an axiom that `next()` returns `Option<i32>`.

3. Create `crates/analysis/tests/impl_trait_tests.rs` with integration tests:
   - Test function with impl Trait return type resolved to concrete Vec<i32> -- postcondition verified
   - Test Ty::Opaque with Iterator bound -- trait bound axiom present in SMT script
   - Test RPITIT: trait with `fn items(&self) -> impl Iterator<Item=i32>` and impl returning `vec.into_iter()` -- behavioral subtyping VC generated
   - Test auto-trait non-leakage: Ty::Opaque does NOT include Send/Sync unless explicit in bounds

4. Run clippy and full test suite.
  </action>
  <verify>
    <automated>cd /Users/alexanderfedin/Projects/hapyy/rust-fv && cargo test -p rust-fv-analysis --test impl_trait_tests 2>&1 | tail -10</automated>
  </verify>
  <done>RPITIT behavioral subtyping wiring generates Liskov VCs for opaque-to-concrete return types, Ty::Opaque trait bound axioms emitted in SMT preamble, 5+ integration tests pass, clippy clean</done>
</task>

</tasks>

<verification>
- `cargo test -p rust-fv-analysis -p rust-fv-driver` -- all existing tests pass
- `cargo test -p rust-fv-analysis --test impl_trait_tests` -- new impl Trait tests pass
- `cargo clippy --all-targets -p rust-fv-analysis -p rust-fv-driver` -- no warnings
- Ty::Opaque appears in ir.rs with proper documentation
- encode_sort.rs handles Ty::Opaque as uninterpreted sort
</verification>

<success_criteria>
- impl Trait return types resolved to concrete types from MIR during conversion
- Unresolvable opaque types fall back to Ty::Opaque with trait bound axioms (sound but incomplete)
- RPITIT contracts verified via existing behavioral subtyping Liskov checks
- Auto-trait information NOT leaked from opaque types
- All tests pass, clippy clean
</success_criteria>

<output>
After completion, create `.planning/phases/52-advanced-type-system-features/52-01-SUMMARY.md`
</output>

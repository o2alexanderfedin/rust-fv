---
phase: 49-cross-crate-interop-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/driver/src/callbacks.rs
  - crates/driver/tests/cross_crate_mono_test.rs
autonomous: true
requirements: [COMPL-10]
must_haves:
  truths:
    - "A generic function called with concrete type args from an external crate appears in MonomorphizationRegistry"
    - "Cross-crate generic instantiations produce monomorphized VC sets"
    - "Uncontracted cross-crate generic callees emit V060 opaque callee warning"
  artifacts:
    - path: "crates/driver/src/callbacks.rs"
      provides: "Extended populate_monomorphization_registry scanning external DefIds"
      contains: "is_local"
    - path: "crates/driver/tests/cross_crate_mono_test.rs"
      provides: "Cross-crate monomorphization E2E test"
      contains: "MonomorphizationRegistry"
  key_links:
    - from: "crates/driver/src/callbacks.rs"
      to: "MonomorphizationRegistry"
      via: "populate_monomorphization_registry scanning Call terminators with external DefIds"
      pattern: "is_local.*false"
---

<objective>
Extend the MonomorphizationRegistry to capture generic instantiations from cross-crate call sites, not just local ones. When a local function calls an external generic function with concrete type args, the registry records that instantiation and VCGen produces monomorphized VCs for it.

Purpose: COMPL-10 -- cross-crate generic instantiations must be fully captured for verification completeness.
Output: Extended populate_monomorphization_registry + cross-crate E2E test
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/driver/src/callbacks.rs (populate_monomorphization_registry at line ~1443)
@crates/analysis/src/monomorphize.rs
</context>

<interfaces>
<!-- Key types the executor needs -->

From crates/analysis/src/monomorphize.rs:
```rust
pub struct TypeInstantiation {
    pub generic_fn_name: String,
    pub substitutions: HashMap<String, Ty>,
    pub label: String,
    pub caller: String,
}
pub struct MonomorphizationRegistry { ... }
impl MonomorphizationRegistry {
    pub fn new() -> Self;
    pub fn register(&mut self, inst: TypeInstantiation);
    pub fn get_instantiations(&self, fn_name: &str) -> &[TypeInstantiation];
}
```

From crates/driver/src/callbacks.rs (existing populate_monomorphization_registry):
- Scans `tcx.hir_body_owners()` for Call terminators
- Extracts `FnDef(callee_def_id, generic_args)` from constant operands
- Maps generic params to concrete types via `mir_converter::convert_ty`
- Skips unresolvable params (still Param or Generic)
- Currently only processes local functions -- needs extension to include external DefIds
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend populate_monomorphization_registry for cross-crate DefIds</name>
  <files>crates/driver/src/callbacks.rs, crates/driver/tests/cross_crate_mono_test.rs</files>
  <behavior>
    - Test 1: The function scans Call terminators where callee DefId is NOT local (is_local() returns false) and still registers the instantiation
    - Test 2: A cross-crate generic call site with concrete type args appears in MonomorphizationRegistry with correct substitutions
    - Test 3: When cross-crate callee has no contract in ContractDatabase, V060 opaque callee diagnostic is emitted (reuse existing V060 infrastructure)
    - Test 4: Duplicate cross-crate instantiations (same fn + same type args from multiple call sites) are deduplicated in the registry
  </behavior>
  <action>
In `populate_monomorphization_registry` (callbacks.rs ~line 1443), the current implementation already scans all local MIR body owners and examines Call terminators. The key change: currently it likely only registers instantiations where the callee is local OR already handles external. Verify the current behavior:

1. Check if the existing code already handles external DefIds -- if `callee_def_id.is_local()` is NOT checked as a filter, the function may already capture cross-crate calls. If it IS filtering to local only, remove that filter.

2. Add an E2E integration test `cross_crate_mono_test.rs` that:
   - Creates an IR `Function` simulating a local function that calls a cross-crate generic function (e.g., `std::cmp::max::<i32>`)
   - Verifies the MonomorphizationRegistry contains the instantiation with `T=i32`
   - Verifies the V060 diagnostic fires for uncontracted external generics

3. Ensure deduplication: if the same external generic function is called with the same type args from multiple local functions, only one TypeInstantiation entry is registered.

4. Run `cargo clippy` and `cargo test` to verify no regressions.
  </action>
  <verify>
    <automated>cd /Users/alexanderfedin/Projects/hapyy/rust-fv && cargo test -p rust-fv-driver cross_crate_mono -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>Cross-crate generic instantiations are captured in MonomorphizationRegistry; E2E test confirms registry population and V060 warning for uncontracted callees</done>
</task>

<task type="auto">
  <name>Task 2: Verify monomorphized VC generation for cross-crate generics</name>
  <files>crates/driver/src/callbacks.rs, crates/driver/tests/cross_crate_mono_test.rs</files>
  <action>
Add an integration test verifying the full pipeline: cross-crate generic call site -> MonomorphizationRegistry entry -> monomorphized VC generation via `generate_vcs_monomorphized`. The test should:

1. Construct a scenario where a local function calls a cross-crate generic with concrete types
2. Verify that `generate_vcs_monomorphized` produces VCs with the substitution applied (no Ty::Generic remaining in the VC set)
3. If the external callee has a contract in ContractDatabase, the monomorphized VC uses the substituted contract

Also add a structural source test (matching the pattern from Phase 44) that verifies `populate_monomorphization_registry` does NOT filter by `callee_def_id.is_local()`.

Run full test suite: `cargo test -p rust-fv-driver` and `cargo clippy -p rust-fv-driver`.
  </action>
  <verify>
    <automated>cd /Users/alexanderfedin/Projects/hapyy/rust-fv && cargo test -p rust-fv-driver -- --nocapture 2>&1 | tail -20 && cargo clippy -p rust-fv-driver -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <done>Full pipeline from cross-crate generic call site to monomorphized VC generation is verified; no local-only filter blocks external DefIds; clippy clean</done>
</task>

</tasks>

<verification>
- `cargo test -p rust-fv-driver cross_crate_mono` passes
- `cargo test -p rust-fv-driver` full suite passes (no regressions)
- `cargo clippy -p rust-fv-driver -- -D warnings` clean
</verification>

<success_criteria>
Cross-crate generic instantiations are captured in MonomorphizationRegistry and produce monomorphized VC sets. Uncontracted cross-crate generics emit V060 warning. E2E test confirms the full pipeline.
</success_criteria>

<output>
After completion, create `.planning/phases/49-cross-crate-interop-completeness/49-01-SUMMARY.md`
</output>

---
phase: 47-mir-coverage-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/analysis/src/ir.rs
  - crates/analysis/src/encode_term.rs
  - crates/analysis/src/vcgen.rs
  - crates/driver/src/callbacks.rs
  - crates/driver/src/diagnostics.rs
  - crates/analysis/tests/cast_kind_tests.rs
  - crates/driver/tests/alignment_e2e.rs
autonomous: true
requirements: [COMPL-02, COMPL-03]

must_haves:
  truths:
    - "A PtrToPtr cast generates an AlignmentSafety VC asserting addr % align == 0"
    - "Each CastKind variant follows a distinct encoding path with distinct SMT output"
    - "IntToInt, IntToFloat, FloatToInt, FloatToFloat, PtrToPtr are all separate enum variants"
    - "Unit tests cover all five CastKind variants with structural Term comparison AND SMT-LIB snapshot"
  artifacts:
    - path: "crates/analysis/src/ir.rs"
      provides: "CastKind::PtrToPtr variant (renamed from Pointer), VcKind::AlignmentSafety variant"
      contains: "PtrToPtr"
    - path: "crates/analysis/src/encode_term.rs"
      provides: "PtrToPtr encoding returning src + alignment VC side-channel"
      contains: "encode_ptr_to_ptr_cast"
    - path: "crates/analysis/tests/cast_kind_tests.rs"
      provides: "Unit tests for all 5 CastKind variants"
      min_lines: 80
    - path: "crates/driver/tests/alignment_e2e.rs"
      provides: "E2E test for aligned/unaligned pointer cast verification"
      min_lines: 40
  key_links:
    - from: "crates/analysis/src/encode_term.rs"
      to: "crates/analysis/src/ir.rs"
      via: "CastKind::PtrToPtr match arm"
      pattern: "CastKind::PtrToPtr"
    - from: "crates/analysis/src/vcgen.rs"
      to: "crates/analysis/src/encode_term.rs"
      via: "encode_cast call in encode_rvalue_for_assignment"
      pattern: "encode_cast"
    - from: "crates/driver/src/callbacks.rs"
      to: "VcKind::AlignmentSafety"
      via: "vc_kind_to_string match arm"
      pattern: "AlignmentSafety.*alignment_safety"
---

<objective>
Rename CastKind::Pointer to CastKind::PtrToPtr, add VcKind::AlignmentSafety, and make PtrToPtr the only cast kind that emits a side-effect alignment safety VC. All five CastKind variants produce structurally distinct SMT output verified by unit tests.

Purpose: COMPL-02 requires pointer alignment VCs on PtrToPtr casts; COMPL-03 requires unambiguous CastKind encoding with distinct SMT shapes per variant.
Output: Updated ir.rs enum, encode_term.rs PtrToPtr handler with alignment VC, unit tests for all 5 variants, E2E alignment test.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-mir-coverage-hardening/47-CONTEXT.md

@crates/analysis/src/ir.rs
@crates/analysis/src/encode_term.rs
@crates/analysis/src/vcgen.rs
@crates/driver/src/callbacks.rs
@crates/driver/src/diagnostics.rs

<interfaces>
<!-- Key types and contracts the executor needs -->

From crates/analysis/src/ir.rs (line 839):
```rust
pub enum CastKind {
    IntToInt,
    IntToFloat,
    FloatToInt,
    FloatToFloat,
    Pointer,  // RENAME to PtrToPtr
}
```

From crates/analysis/src/ir.rs (line 162):
```rust
pub enum UnsafeOperation {
    PtrCast { from_ty: Ty, to_ty: Ty },
    // ...
}
```

From crates/analysis/src/encode_term.rs (line 718):
```rust
pub fn encode_cast(
    kind: &CastKind, src: Term, from_bits: u32, to_bits: u32,
    from_signed: bool, to_signed: bool,
) -> Term
```

From crates/analysis/src/vcgen.rs (line 86):
```rust
pub enum VcKind {
    // ... existing variants ...
    MemorySafety,  // model for new AlignmentSafety
    // ...
}
```

From crates/driver/src/callbacks.rs (line 1657):
```rust
fn vc_kind_to_string(vc_kind: &VcKind) -> String
// Must add AlignmentSafety => "alignment_safety" arm
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename CastKind::Pointer to PtrToPtr, add VcKind::AlignmentSafety, and implement alignment VC encoding</name>
  <files>crates/analysis/src/ir.rs, crates/analysis/src/encode_term.rs, crates/analysis/src/vcgen.rs, crates/driver/src/callbacks.rs, crates/driver/src/diagnostics.rs, crates/analysis/tests/cast_kind_tests.rs</files>
  <behavior>
    - Test: encode_cast with CastKind::PtrToPtr returns the source term (identity) — same as old Pointer behavior
    - Test: encode_cast with IntToInt produces Term::ZeroExtend or Term::SignExtend or Term::Extract (distinct from PtrToPtr identity)
    - Test: encode_cast with IntToFloat produces a distinct FpFromBv or equivalent term
    - Test: encode_cast with FloatToInt produces a distinct BvFromFp or equivalent term
    - Test: encode_cast with FloatToFloat produces a distinct FpConvert or equivalent term
    - Test: All 5 variants produce structurally different Term outputs (no two share the same shape)
    - Test: SMT-LIB string serialization of each variant's output differs (snapshot regression)
    - Test: VcKind::AlignmentSafety serializes to "alignment_safety" string
  </behavior>
  <action>
  1. In `ir.rs`: Rename `CastKind::Pointer` to `CastKind::PtrToPtr`. Add `VcKind::AlignmentSafety` variant with doc-comment: "Pointer alignment check (addr % align == 0 for PtrToPtr casts)".

  2. Fix all compilation errors from the rename — grep for `CastKind::Pointer` across the codebase and update to `CastKind::PtrToPtr`. Key locations: `encode_term.rs:732`, `mir_converter.rs` (if any), `vcgen.rs`.

  3. In `encode_term.rs`: Change the `CastKind::PtrToPtr => src` arm to call a new `encode_ptr_to_ptr_cast(src)` function that still returns `src` (identity). The alignment VC generation will be a side-effect emitted in vcgen.rs where the cast is consumed, not inside encode_cast itself (encode_cast returns a Term, not a list of VCs). Add a doc-comment explaining PtrToPtr is the only cast kind with a side-effect safety VC emitted at the call site.

  4. In `vcgen.rs` at the `Rvalue::Cast` arm in `encode_rvalue_for_assignment` (~line 1716): After calling `encode_cast` for `CastKind::PtrToPtr`, also return alignment VC information. The approach: modify `encode_rvalue_for_assignment` to return `Option<(Term, Vec<VerificationCondition>)>` instead of `Option<Term>`, OR add a separate function `alignment_vcs_for_cast` that the caller can invoke. The simpler approach: add a helper `fn alignment_vc_for_ptr_cast(src_operand: &Operand, target_ty: &Ty, func: &Function) -> Option<VerificationCondition>` that creates an `Assert((= (mod addr align) 0))` VC with `VcKind::AlignmentSafety`. Call this from `encode_assignment` or the rvalue dispatch site where VCs are accumulated. The alignment value comes from `target_ty` — use `ty_alignment(target_ty)` helper returning alignment in bytes (1 for u8, 4 for u32, 8 for u64/pointers).

  5. In `callbacks.rs`: Add `VcKind::AlignmentSafety => "alignment_safety"` to `vc_kind_to_string`. In `diagnostics.rs`: Add V070 error code handling for AlignmentSafety (Warning severity, like MemorySafety).

  6. Create `crates/analysis/tests/cast_kind_tests.rs` with unit tests:
     - One test per CastKind variant verifying the Term structure
     - A cross-variant test asserting all 5 produce distinct SMT-LIB output
     - A VcKind::AlignmentSafety serialization test
  </action>
  <verify>
    <automated>cd /Users/alexanderfedin/Projects/hapyy/rust-fv && cargo test --package rust-fv-analysis cast_kind -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>CastKind::Pointer renamed to PtrToPtr, VcKind::AlignmentSafety added, all 5 CastKind variants produce structurally distinct SMT output, unit tests pass, entire crate compiles cleanly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: E2E alignment VC verification test</name>
  <files>crates/driver/tests/alignment_e2e.rs</files>
  <behavior>
    - Test: A function casting *const u8 to *const u32 (aligned address) produces AlignmentSafety VC that verifies (UNSAT)
    - Test: A function casting *const u8 to *const u32 (unaligned address) produces AlignmentSafety VC that Z3 finds counterexample (SAT)
  </behavior>
  <action>
  Create `crates/driver/tests/alignment_e2e.rs` following the Phase 46-03 IsTester E2E test pattern (from `crates/driver/tests/e2e_verification.rs` or similar):

  1. Build a synthetic IR `Function` that performs a `Rvalue::Cast(CastKind::PtrToPtr, ...)` from a `*const u8` operand to `*const u32` type.

  2. Generate VCs with `generate_vcs` and assert that at least one VC has `vc_kind == VcKind::AlignmentSafety`.

  3. For the aligned case: add a precondition asserting `addr % 4 == 0`, verify the alignment VC is UNSAT (aligned pointer passes).

  4. For the unaligned case: add a precondition asserting `addr % 4 == 1` (or no alignment precondition), verify the alignment VC is SAT with a counterexample showing the misaligned address.

  Use the established E2E test infrastructure — import from `rust_fv_analysis::vcgen::{generate_vcs, VcKind}` and `rust_fv_analysis::ir::*`.
  </action>
  <verify>
    <automated>cd /Users/alexanderfedin/Projects/hapyy/rust-fv && cargo test --package rust-fv-driver alignment_e2e -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>E2E test confirms aligned PtrToPtr cast verifies (UNSAT) and unaligned PtrToPtr cast produces counterexample (SAT), matching COMPL-02 success criterion</done>
</task>

</tasks>

<verification>
- `cargo test --package rust-fv-analysis` passes (no regressions from CastKind rename)
- `cargo test --package rust-fv-driver` passes (alignment E2E + existing tests)
- `cargo clippy --all-targets` produces no new warnings
- grep confirms no remaining `CastKind::Pointer` references in codebase
</verification>

<success_criteria>
- CastKind has 5 variants: IntToInt, IntToFloat, FloatToInt, FloatToFloat, PtrToPtr
- VcKind::AlignmentSafety exists and is wired to diagnostics
- PtrToPtr cast generates alignment VC (addr % align == 0)
- All 5 CastKind variants produce structurally distinct SMT output
- E2E: aligned pointer cast verifies, unaligned produces counterexample
</success_criteria>

<output>
After completion, create `.planning/phases/47-mir-coverage-hardening/47-01-SUMMARY.md`
</output>

---
phase: 47-mir-coverage-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/analysis/src/vcgen.rs
  - crates/analysis/src/encode_term.rs
  - .planning/phases/47-mir-coverage-hardening/AUDIT.md
autonomous: true
requirements: [COMPL-12]

must_haves:
  truths:
    - "Every match arm in vcgen.rs either generates a VC or has an explicit doc-comment explaining why it is skipped"
    - "Every match arm in encode_term.rs either encodes a term or has an explicit doc-comment explaining the skip"
    - "No silent fallthrough remains undocumented in the VC generation pipeline"
    - "Wildcard catch-all arms are replaced with explicit variant listings where practical"
    - "A centralized audit table exists at the top of vcgen.rs as a module-level doc-comment"
  artifacts:
    - path: "crates/analysis/src/vcgen.rs"
      provides: "Documented match arms with audit table"
      contains: "Intentionally no VC"
    - path: "crates/analysis/src/encode_term.rs"
      provides: "Documented match arms in encoding pipeline"
      contains: "Intentionally no VC"
    - path: ".planning/phases/47-mir-coverage-hardening/AUDIT.md"
      provides: "Formal phase audit record"
      min_lines: 30
  key_links:
    - from: "crates/analysis/src/vcgen.rs"
      to: "module-level doc-comment"
      via: "//! audit table"
      pattern: "//!.*Match Arm Audit"
---

<objective>
Audit every match arm in vcgen.rs and encode_term.rs for silent fallthrough. Document every intentionally-skipped arm with inline doc-comments, replace wildcard catch-alls with explicit variant listings where practical, and create a centralized audit table.

Purpose: COMPL-12 requires that no silent match arm fallthrough remains undocumented in the VC generation pipeline.
Output: Documented vcgen.rs + encode_term.rs match arms, module-level audit table, formal AUDIT.md record.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-mir-coverage-hardening/47-CONTEXT.md

@crates/analysis/src/vcgen.rs
@crates/analysis/src/encode_term.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and document all match arms in vcgen.rs</name>
  <files>crates/analysis/src/vcgen.rs</files>
  <action>
  1. **Enumerate all match expressions** in vcgen.rs that dispatch on IR types (Rvalue, Statement, Terminator, BinOp, UnaryOp, VcKind, CastKind, Ty, etc.). Focus on matches that determine whether a VC is generated or skipped.

  2. **For each wildcard catch-all arm (`_ => ...`):**
     - If the set of covered variants is small (<=10), replace `_` with explicit variant listings so the compiler enforces completeness on future API changes.
     - If the set is large and explicit listing causes excessive churn, keep the catch-all but add a doc-comment listing all currently-covered variants: `/// Intentionally no VC: covers [Variant1, Variant2, ...] — [reason]`

  3. **For each arm that returns None, does nothing, or falls through:**
     - Add inline doc-comment directly above: `/// Intentionally no VC: [specific reason why this arm needs no verification condition]`
     - Reasons should be technical and specific, e.g.: "Aggregate construction is handled by encode_assignment's datatype path, not the rvalue dispatcher"

  4. **Create centralized audit table** as a module-level doc-comment (`//!`) near the top of vcgen.rs (after the existing module doc but before `use` statements):
     ```
     //! ## Match Arm Audit Table (COMPL-12)
     //!
     //! | Location | Match Subject | Arms with No VC | Reason |
     //! |----------|---------------|-----------------|--------|
     //! | line NNN | Rvalue variant | Aggregate, Repeat | handled by encode_assignment |
     //! | ... | ... | ... | ... |
     ```

  5. **Key match expressions to audit** (non-exhaustive, find ALL):
     - `encode_rvalue_for_assignment` (~line 1689): match on Rvalue variants
     - `encode_assignment` block: match on Statement/Rvalue
     - Terminator dispatch (~line 1237): match on Terminator variants
     - `encode_overflow_vcs` match on BinOp
     - Any match on `VcKind` for diagnostics/reporting
     - SwitchInt dispatch for discriminants
  </action>
  <verify>
    <automated>cd /Users/alexanderfedin/Projects/hapyy/rust-fv && cargo test --package rust-fv-analysis -- --nocapture 2>&1 | tail -5 && cargo clippy --package rust-fv-analysis -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <done>Every match arm in vcgen.rs that skips VC generation has an explicit doc-comment; wildcard catch-alls replaced with explicit variants where practical; centralized audit table at module top; all existing tests still pass</done>
</task>

<task type="auto">
  <name>Task 2: Audit encode_term.rs match arms and create formal AUDIT.md</name>
  <files>crates/analysis/src/encode_term.rs, .planning/phases/47-mir-coverage-hardening/AUDIT.md</files>
  <action>
  1. **Audit encode_term.rs match expressions** using the same methodology as Task 1:
     - `encode_cast` (~line 718): match on CastKind — document each arm's SMT output shape
     - `encode_binop`: match on BinOp — document any skipped operators
     - `encode_unop`: match on UnaryOp — document any fallthrough
     - `encode_term` / `encode_operand` functions: any match on Term, Operand, Ty
     - Any wildcard catch-all arms

  2. **For each undocumented skip/fallthrough**, add:
     `/// Intentionally no encoding: [reason]`

  3. **Replace wildcard catch-alls** with explicit variant listings where practical (same rules as Task 1).

  4. **Create `.planning/phases/47-mir-coverage-hardening/AUDIT.md`** — the formal phase audit record:
     ```markdown
     # Match Arm Fallthrough Audit — Phase 47

     ## Scope
     - crates/analysis/src/vcgen.rs
     - crates/analysis/src/encode_term.rs

     ## Findings

     ### vcgen.rs
     | Line | Match Subject | Skipped Arms | Reason | Action |
     |------|---------------|-------------|--------|--------|
     | ... | ... | ... | ... | documented / replaced wildcard |

     ### encode_term.rs
     | Line | Match Subject | Skipped Arms | Reason | Action |
     |------|---------------|-------------|--------|--------|
     | ... | ... | ... | ... | documented / replaced wildcard |

     ## Summary
     - Total match expressions audited: N
     - Arms with no VC/encoding: M (all documented)
     - Wildcard catch-alls replaced: K
     - Wildcard catch-alls kept (documented): J
     ```
  </action>
  <verify>
    <automated>cd /Users/alexanderfedin/Projects/hapyy/rust-fv && cargo test --package rust-fv-analysis -- --nocapture 2>&1 | tail -5 && cargo clippy --package rust-fv-analysis -- -D warnings 2>&1 | tail -5 && test -f .planning/phases/47-mir-coverage-hardening/AUDIT.md && echo "AUDIT.md exists"</automated>
  </verify>
  <done>All match arms in encode_term.rs documented; AUDIT.md created with complete audit findings for both files; no silent fallthrough remains in the VC generation pipeline</done>
</task>

</tasks>

<verification>
- `cargo test --package rust-fv-analysis` passes (audit is documentation-only, no logic changes)
- `cargo clippy --package rust-fv-analysis` passes with no new warnings
- grep for `_ =>` in vcgen.rs and encode_term.rs — each remaining wildcard has a doc-comment above it
- AUDIT.md exists and contains findings for both files
</verification>

<success_criteria>
- Every match arm in vcgen.rs that skips VC generation has an inline doc-comment
- Every match arm in encode_term.rs that skips encoding has an inline doc-comment
- Centralized audit table exists at top of vcgen.rs as //! module doc
- Wildcard catch-alls replaced with explicit variants where practical
- AUDIT.md formal record created with complete findings
- No silent fallthrough remains undocumented
</success_criteria>

<output>
After completion, create `.planning/phases/47-mir-coverage-hardening/47-02-SUMMARY.md`
</output>

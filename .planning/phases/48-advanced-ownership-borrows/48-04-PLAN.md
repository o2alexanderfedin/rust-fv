---
phase: 48-advanced-ownership-borrows
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/driver/src/mir_converter.rs
  - crates/driver/tests/two_phase_borrow_e2e.rs
autonomous: true
gap_closure: true
requirements: [COMPL-13]

must_haves:
  truths:
    - "MIR converter detects BorrowKind::Mut with TwoPhaseBorrow and sets BorrowInfo.phase = BorrowPhase::Reserved"
    - "Reserved borrows transition to Activated at the consuming call site terminator"
    - "A function using vec.push(vec.len()) pattern produces no false-positive BorrowConflict VC end-to-end"
  artifacts:
    - path: "crates/driver/src/mir_converter.rs"
      provides: "TwoPhaseBorrow detection and BorrowInfo population"
      contains: "BorrowPhase::Reserved"
    - path: "crates/driver/tests/two_phase_borrow_e2e.rs"
      provides: "E2E integration test for two-phase borrow pattern"
      min_lines: 40
  key_links:
    - from: "crates/driver/src/mir_converter.rs"
      to: "crates/analysis/src/ir.rs"
      via: "BorrowInfo with BorrowPhase::Reserved populated from MIR flag"
      pattern: "BorrowPhase::Reserved"
    - from: "crates/driver/src/mir_converter.rs"
      to: "crates/analysis/src/borrow_conflict.rs"
      via: "borrow_info vec consumed by detect_borrow_conflicts"
      pattern: "borrow_info"
---

<objective>
Close the COMPL-13 gap: wire TwoPhaseBorrow MIR flag detection through mir_converter.rs so that
two-phase borrow patterns (e.g., `vec.push(vec.len())`) populate `BorrowPhase::Reserved` on the
`BorrowInfo` struct. The analysis-level skip logic already works (5 tests pass in
borrow_conflict.rs), but the MIR converter never populates `BorrowPhase::Reserved`, causing
all borrows to default to `BorrowPhase::Active` and producing false-positive conflict VCs
on real MIR input.

Purpose: Complete the end-to-end two-phase borrow pipeline so real Rust code is verified correctly.
Output: Updated mir_converter.rs + E2E integration test proving no false positive.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-advanced-ownership-borrows/48-02-SUMMARY.md

<interfaces>
<!-- Key types the executor needs from ir.rs -->

From crates/analysis/src/ir.rs:
```rust
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum BorrowPhase {
    Active,
    Reserved,
    Activated,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct BorrowInfo {
    pub local_name: String,
    pub region: String,
    pub is_mutable: bool,
    pub deref_level: usize,
    pub source_local: Option<String>,
    pub phase: BorrowPhase,
}
```

From crates/analysis/src/borrow_conflict.rs (line 131):
```rust
// Two-phase borrow support: Reserved borrows skip conflict check
if mutable.phase == crate::ir::BorrowPhase::Reserved {
    continue;
}
```

From crates/driver/src/mir_converter.rs (line 436-441, current code):
```rust
mir::Rvalue::Ref(_, borrow_kind, place) => {
    let mutability = match borrow_kind {
        mir::BorrowKind::Mut { .. } => ir::Mutability::Mutable,
        _ => ir::Mutability::Shared,
    };
    Some(ir::Rvalue::Ref(mutability, convert_place(place)))
}
```

Function struct (line 273-299) initializes `borrow_info: vec![]` — never populated.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Populate BorrowInfo from MIR borrow statements with TwoPhaseBorrow detection</name>
  <files>crates/driver/src/mir_converter.rs, crates/driver/tests/two_phase_borrow_e2e.rs</files>
  <behavior>
    - Test 1: A function with a standard `&mut x` borrow produces a BorrowInfo with phase=Active
    - Test 2: A function with a TwoPhaseBorrow MIR borrow produces a BorrowInfo with phase=Reserved
    - Test 3: When a Reserved borrow's destination local appears as an arg in a Call terminator, its phase transitions to Activated
    - Test 4: A function with the `vec.push(vec.len())` pattern produces no BorrowConflict VCs (end-to-end)
  </behavior>
  <action>
Modify `convert_function` in `crates/driver/src/mir_converter.rs` to:

1. **Collect borrow info from MIR statements:** After converting basic blocks (line 248-252), iterate over all MIR basic blocks and statements. For each `StatementKind::Assign(place, Rvalue::Ref(_, borrow_kind, borrowed_place))`:
   - Extract the destination local name (e.g., `_3`)
   - Determine mutability from borrow_kind
   - Detect two-phase: match `mir::BorrowKind::Mut { kind: mir::MutBorrowKind::TwoPhaseBorrow }` to set `phase = BorrowPhase::Reserved`; all other mutable borrows get `BorrowPhase::Active`; shared borrows get `BorrowPhase::Active`
   - Push a `BorrowInfo { local_name, region: format!("'_{}", idx), is_mutable, deref_level: 0, source_local: None, phase }` to a `borrow_infos` vec

2. **Activate reserved borrows at call sites:** After collecting borrows, iterate MIR basic block terminators. For each `TerminatorKind::Call { args, .. }`, check if any arg operand references a local that has a Reserved borrow. If so, change that borrow's phase to `BorrowPhase::Activated`.

3. **Wire into Function struct:** Replace `borrow_info: vec![]` with `borrow_info: borrow_infos` at line 285.

Create `crates/driver/tests/two_phase_borrow_e2e.rs` with an integration test that constructs an `ir::Function` manually simulating the `vec.push(vec.len())` pattern:
- One mutable borrow with `BorrowPhase::Reserved` (the `&mut vec` for push)
- One shared borrow with `BorrowPhase::Active` (the `&vec` for len)
- Both borrows have overlapping live ranges
- Call `detect_borrow_conflicts` and assert NO conflicts are returned (the Reserved phase causes the skip)
- Then set the mutable borrow to `BorrowPhase::Active` and assert a conflict IS returned (control case)

Run `cargo clippy -p rust-fv-driver` and `cargo fmt -p rust-fv-driver` after changes.
  </action>
  <verify>
    <automated>cd /Users/alexanderfedin/Projects/hapyy/rust-fv && cargo test -p rust-fv-driver --test two_phase_borrow_e2e -- --nocapture 2>&1 | tail -20 && cargo test -p rust-fv-analysis --test two_phase_borrow_tests -- --nocapture 2>&1 | tail -10 && cargo clippy -p rust-fv-driver -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <done>
    - mir_converter.rs populates borrow_info with BorrowPhase::Reserved for TwoPhaseBorrow MIR borrows
    - Reserved borrows are transitioned to Activated at call site terminators
    - E2E test confirms no false-positive BorrowConflict for overlapping Reserved+Shared borrows
    - E2E test confirms conflict IS detected when both are Active (control case)
    - All existing two_phase_borrow_tests still pass
    - clippy clean
  </done>
</task>

</tasks>

<verification>
1. `cargo test -p rust-fv-driver --test two_phase_borrow_e2e` passes (new E2E test)
2. `cargo test -p rust-fv-analysis --test two_phase_borrow_tests` passes (existing 5 tests)
3. `cargo test -p rust-fv-analysis --test borrow_splitting_tests` passes (existing 6 tests)
4. `cargo clippy -p rust-fv-driver -- -D warnings` clean
5. `cargo clippy -p rust-fv-analysis -- -D warnings` clean
6. grep confirms `BorrowPhase::Reserved` appears in mir_converter.rs
</verification>

<success_criteria>
- COMPL-13 gap closed: MIR converter detects TwoPhaseBorrow and populates BorrowPhase::Reserved
- Reserved borrows activate at call site terminators
- End-to-end test proves vec.push(vec.len()) pattern produces no false-positive conflict VC
- All existing phase 48 tests remain green (35 tests across 5 test files)
</success_criteria>

<output>
After completion, create `.planning/phases/48-advanced-ownership-borrows/48-04-SUMMARY.md`
</output>
